# 更新日志

## v0.2.1 (2026-09-18)

- **增强 `-p` 与 `-m` 参数的容错清洗与标点兼容**：
  - 新增 `_clean_param_val()` 参数清洗机制，自动去除用户可能误带的外层尖括号（`< >`）、书名号（`《 》`）、中文方括号（`【 】`）、引号及末尾逗号/分号（如 `-p <动漫风>` 自动识别为 `动漫风`，`-m <5>` 自动识别为 `5` 并解析为 V5 模型）。
  - 扩展模型正则匹配规则为 `\S+`，防止尖括号等特殊字符被截断。
- **新增独立的全局模型查询与切换命令**：
  - 新增 `/nai model`（或 `/nai 模型`）：查询当前默认模型，并列出常用切换命令。
  - 新增 `/nai model <模型序号/名称>`（如 `/nai model 5`、`/nai model 5c`、`/nai model 4.5`）：一键切换全局默认生图模型并自动保存。
- **支持单独发送 `-m` / `-p` 快捷设置**：
  - 当用户单独发送 `/nai -m 5` 或 `/nai -p 动漫风`（未附带提示词）时，自动识别并将其作为快捷设置处理（分别切换默认模型或默认预设），不再报“提示词不能为空”。
- **智能化未输入提示词引导**：
  - 当用户在生图调用中仅传入了 `-p` 预设或 `-m` 模型却漏掉描述词时，返回友好的双重操作指引（引导用户单次加词生图，或指导其设为全局默认）。
- **完善自动化测试**：
  - 增加对符号清洗、尖括号参数提取、独立切换子命令及智能引导的单元测试覆盖。

## v0.2.0 (2026-09-18)

- **支持 NovelAI V5 双模型**：
  - 新增 `nai-diffusion-5-full` (NovelAI Diffusion V5 Full) 和 `nai-diffusion-5-curated` (NovelAI Diffusion V5 Curated) 支持。
  - 在配置项 `default_model` 中将 V5 双模型排在最前，并在配置提示中说明其二次元细节与自然语言支持特性。
- **指令 `-m` / `--model` 快速切换模型**：
  - `/nai` 指令增加 `-m` / `--model` 参数支持，支持使用别名快速指定生图模型（如 `-m 5`、`-m 5c`、`-m 4.5`、`-m furry` 等）。
  - 提供 `MODEL_ALIASES` 字典与 `resolve_model_alias()` 解析函数，智能映射模型简写与全称。
  - 提供 `is_v5_model()` 判断模型是否属于 V5 架构。
- **LLM 生图工具扩展**：
  - `nai_generate_tool` 增加 `model` 参数，支持大模型在调用生图工具时指定所用模型。
- **生图信息与扣点提示**：
  - 生图信息标签中增加当前所用模型标识（如使用 V5 或显式指定模型时提示）。
  - `/nai balance` 余额查询中增加 V4.5 (1点/张) 与 V5 (5点/张) 普通尺寸预计可生成张数区别说明。

## v0.1.2 (2026-09-18)

- **修复生图完成后的图片保存异常**：
  - 修复 `main.py::_do_generate` 中调用 `ImageManager.save_image()` 时误传 `prompt=prompt` 导致 `unexpected keyword argument 'prompt'` 的缺陷。
  - 修复 `save_image()` 未使用 `await` 异步等待的问题。
  - `ImageManager.save_image()` 签名增加 `**kwargs` 兼容，防止任何未声明的关键字传参报错。

## v0.1.1 (2026-09-18)

- **修复 WebUI 面板 Bridge 通信与路由问题**：
  - 前端引入异步 Bridge 轮询解析器（`resolveBridge`）并改用 `type="module"`，确保在 AstrBot 仪表盘 iframe 异步注入 `bridge-sdk.js` 后能稳定建立通信，消除「无法与 AstrBot 建立通信」的提示。
  - 前端增加 `bridge.ready()` 与 `bridge.onContext()` 主题和生命周期同步。
  - 后端 Web API 同步注册带有 `/{PLUGIN_NAME}/` 前缀的官方标准路由与相对路由，保证不同 AstrBot 路由转发器都能精确匹配。

## v0.1.0 (2026-09-18)

- **独立 WebUI 预设管理面板**：
  - 在 AstrBot 插件管理页面中增加独立的「NovelAI 预设管理」页面（`pages/nai-config/`），通过 AstrBot 原生 Pages 机制与 Web API 桥接，无需手动改写配置文件。
- **三段式预设模型**：
  - 预设全面升级为 **质量词/画师串**、**附带正向词**、**附带负向词** 三段式结构：
    - **质量词/画师串**：作为独立 `artist` 参数发送给 Nai2API 网关，保持 NAI 原生语法隔离，画师串中负权重标签（如 `-1.5::xxx::`）自动提取至负面词。
    - **附带正向词**：自动附加在用户提示词末尾，补充风格与光影效果。
    - **附带负向词**：自动与全局默认负向词智能去重合并。
  - 智能构图兜底：当提示词和预设均未指定景别时，自动补充 `full body, standing`。
- **默认预设功能**：
  - 支持在 WebUI 中一键将任意内置或自定义预设设为默认预设，或取消默认预设。
  - 指令端同步增加 `/nai default [预设名]`（或 `/nai 默认 [预设名]`）查询、设置和取消。
  - 支持生图参数 `--no-preset`，在已设置默认预设的情况下可单次临时禁用预设。
- **数据与配置双向互通**：
  - WebUI 面板操作即时回写 AstrBot 插件配置 `custom_presets`（`template_list`）和本地数据文件，与 `/nai save`、`/nai update`、`/nai del` 指令互通共享。
- **实时模拟预览**：
  - WebUI 页面提供测试提示词与实时预览框，所见即所得查看最终组装的 artist / tag / negative 参数。

## v0.0.1 (2026-09-18)

- **全新初始版本**：从原版纯净架构重新起步，版本从 0.0.1 开始迭代。
- 支持 `/nai` 文生图指令（尺寸、预设、画师前缀、负面提示词、随机种子）。
- 支持 5 个官方内置预设（2.5D唯美风、韩漫小清新风、本子动漫风、GalGame风、动漫风）及自定义预设增删改查。
- 支持 LLM 工具调用生图（支持 AI 助手直接调用）。
- 支持生图信息标签、余额查询与本地图片缓存自动清理。
