# 更新日志

> 说明：本仓库自 v1.0.0 起独立计数（原项目基于 helloWKQ/AstrBot_Nai2API 二开，
> 早期内部迭代过 v1.2.0 / v1.3.x，为对接 AstrBot 插件市场，版本号从 1.0.0 重新开始）。

## v1.5.0

新增「**默认预设**」功能，并修掉 WebUI 那个只搬画师串的缺陷。

### 问题

WebUI 预设卡片上的「设为默认画师串」按钮，**只把预设的画师串填进输入框**，两处毛病：

1. **附带正向词、附带负向词全丢**。点击处理里只赋值了「画师串」输入框，从没碰过「负向词」框，也没把附带正向词带过来。v1.4.2 起预设是**三段**（画师串 + 附带正向词 + 附带负向词），这个按钮却只搬了第一段。
2. **这类预设连按钮都不出现**。外层判断写的是 `if (preset.artist)` —— 一个「只有正向/负向词、画师串为空」的预设（v1.4.2 起允许），画师串是空串，按钮直接不渲染，用户根本没法用它。

而且旧按钮的交互本身也别扭：点完只是**填进输入框**，还得自己再点一次「保存」，等于把「用这个预设」变成了「改全局画师串」——和「预设」这个概念是打架的。

### 方案

改成 **「默认预设」** 机制：点一下让**整个预设**成为默认，之后每次生图自动套用它的三段内容。

- 新增配置项 **`default_preset`**（默认空 = 不使用）。填了预设名后，生图时自动套用该预设的画师串 + 附带正向词 + 附带负向词，不用每次写 `-p`
- **临时换别的**：`/nai -p 其它预设名`（显式 `-p` 永远优先）
- **临时完全不用**：`/nai --no-preset`
- WebUI 卡片按钮从「设为默认画师串」改为「**设为默认风格**」，点击即生效；当前默认的那张卡显示 **`默认`** 徽章、按钮变「取消默认」；按钮渲染条件放宽为「三段任意一段非空」
- 「画师串 / 质量前缀」区域顶部加一行状态：`当前默认预设：xxx` / `当前默认预设：未设置`，一眼知道生效的是哪个

实现上加了 `_effective_preset()` 作为**唯一**的解析入口，`/nai` 指令路径和 LLM 工具路径都走它 —— 两条链路各写各的，正是 v1.4.6 之前「图生图静默丢画师串」的成因，这次直接堵死。

**容错底线**：默认预设指向一个已被删掉的预设时，按「无预设」处理并记一条告警，**绝不抛错** —— 用户删个预设不该让生图整个坏掉，代价只能是该功能不生效。存在性判断用 `get_entry()` 而不是 `get()`：`get()` 对「存在但画师串为空」的预设返回 `""`（不是 `None`），用它判断会漏判，正好会漏掉上面第 2 条那种预设。

### 行为变更（**注意**）

- **移除了旧按钮「把预设内容填进上面输入框」的行为**。它已被默认预设取代。原来习惯「点一下 → 再点保存」的用户，现在直接点「设为默认风格」即可；如果只是想借用某个预设的画师串，可以点「预览」看效果，或手动复制
- 面板顶部「当前默认预设」状态行是新增的，未设置时显示「未设置」

### 顺带修

面板左下角显示的版本号一直是 **1.4.3**（写死在代码里的），插件一路升到 1.4.6 它也没变过 —— 用户照着面板报版本号会把排查带偏。现在改成从 `metadata.yaml` 读，读失败退回 `unknown`，绝不抛错。

### 影响面

- **只有配了 `default_preset` 的人**行为会变（需要主动去设）。没配的人（默认值就是空）行为和以前完全一样
- 设了默认预设后，**每次生图**都会套用它的三段。想临时脱离：`--no-preset`
- 预设本身的数据格式**没有改动**，已有的自定义预设全都能直接当默认预设用

### 测试

- `tests/test_prompt_dict_quality.py` **7960/7960 通过**（本次未动词库）
- `tests/test_webui_api.py` 188 → **237 项全过**（+49）：schema 三段（存在 / `type=string` / `default=""`）+ `__init__` 真的读入；`_effective_preset` 六种分支（没 `-p` 套默认 / `-p` 覆盖 / `-p` 优先于 `--no-preset` / `--no-preset` 返 None / 没配返 None / 指向不存在的预设返 None 且不抛错）；「画师串为空、只有正向负向词」的预设能当默认预设（防 `get()` 漏判回归，且额外断言其三段端到端真的生效）；`--no-preset` 能被剥掉、不影响 prompt、**不被 `--artist` / `--negative` 吃掉**（照 main.py 顶部的贪婪匹配坑写）；两条调用链都必须经过 `_effective_preset` 的源码级护栏；`GET config` 带 `default_preset` 且 `version` 来自 `metadata.yaml`（并断言不再是写死的 `1.4.3`）；`POST preset/default` 三态（设置成功 + 内存同步 + 落盘 / 空 name 取消 / 不存在返回 400 且配置未被写入）

## v1.4.6

修一个**静默失效**的缺陷：**图生图会把画师串悄悄丢掉** —— 用户配了画师串，图生图却完全不生效，而且一个字都不提示。

### 问题

用户反馈「画师串画出来不对」：明明配置页里画师串填得好好的，出的图风格却跟画师串对不上。

触发条件是**回复一张图片再发指令**（这时插件会自动切到图生图）。同一个画师串：

- 直接发指令（文生图）→ 画师串生效，风格正确
- 回复带图的消息发指令（图生图）→ 画师串**完全没参与**，出的是渠道模型（如 `gpt-image-2`）自己的风格

用户看到的现象是「时好时坏」，但完全看不出规律 —— 因为两条路径的行为不一致，而界面上没有任何提示。

### 原因

插件有两条生图路径，画师串的传递只做了一半：

| 路径 | 画师串 | 代码位置 |
| --- | --- | --- |
| 文生图（NovelAI） | **拼进提示词最前面** | `core/nai2api_client.py` |
| 图生图（用户自配渠道） | **根本没传** | `main.py` 的 img2img 分支 |

具体是：图生图分支调用 `self.img2img.generate(...)` 时参数列表里没有 `artist=`，而 `core/img2img_client.py` 的 `Img2ImgClient.generate()` 签名里**压根就没有 artist 这个参数**。所以这不是「传了但被忽略」，而是「从头到尾就没打算传」。

**为什么这个 bug 特别难排查**：它没有任何报错、没有任何日志、界面也没有任何变化。用户能观察到的只有「图不对」。要定位到「原来回复带图会走另一条引擎」，得先知道插件内部有两条路径 —— 这对普通用户几乎不可能。

所以这次修复的重点不只是「让画师串能生效」，更是**让「没生效」这件事说得出口**。

### 修复

新增配置项 **`img2img_inherit_artist`（图生图时带上画师串）**，默认 **关闭**：

- **关闭**（默认）：画师串不发送给图生图渠道，但会**记一条日志**，并在图片下方的信息标签里追加（画师串未生效）—— 用户一眼就能看到「我配的画师串这次没用上」
- **开启**：画师串拼在提示词最前面，与文生图行为一致

**为什么默认关闭**：图生图渠道多是 OpenAI 风格接口（`gpt-image-2` 等），不认 NovelAI 的画师串语法（`artist:xxx`、`1.2::tag::`）。硬拼进去，轻则无效，重则干扰模型对正常提示词的理解 —— 反而可能让画面更差。所以默认不发送是更安全的选择，但要**明确告知**。

### 影响面

- 只影响**图生图**路径。文生图的画师串行为完全没有变化
- 默认值（关闭）与 v1.4.5 的实际行为一致（那时候也等于不发送），所以升级后图生图的出图结果**不会变**
- 唯一的变化是：现在会告诉你「画师串没生效」，而不是继续瞒着
- 想让图生图也用上画师串 → 去插件配置页打开「图生图时带上画师串」；或者用 `--no-i2i` 强制走文生图

### 测试

- `tests/test_prompt_dict_quality.py` **7960/7960 通过**（本次未动词库）
- `tests/test_webui_api.py` 163 → **187 项全过**（+24）：schema 开关存在且默认 `false` / 带 `condition` / 未塞进 `img2img.items`；`_build_info_label` 在图生图 + `artist_ignored=True` 时含「画师串未生效」、不丢弃时不出现、默认参数不出现、文生图路径不出现；`_do_generate` 图生图分支在关闭时 prompt 不含画师串、开启时 prompt 以画师串开头（`f"{artist}, {prompt}"`）、画师串为 `None`/空串/全空格时不产生脏拼接；`_send_image_with_info` 标签联动（关闭 + 有串 → 提示、开启 → 不提示、没配串 → 不提示、文生图 → 不提示）

## v1.4.5

修两个 bug：**图生图彻底不可用**（严重）、**QQ 发图超时重试导致图片发两张**。

---

### Bug 1：图生图一次都没成功过（严重）

#### 问题

用户回复一张图再发 `#nai ...`，本该以那张图为参考来画，实际日志是：

```
⚠️ 参考图下载/解码失败：object str can't be used in 'await' expression，本次改为文生图
```

然后默默按文生图出了图 —— 用户完全看不出来哪里不对，只会觉得「图生图好像不太听话」。

#### 原因

一行代码写错了。`_fetch_reference_image()` 里是这么写的：

```python
return await _materialize_image(first), None
```

但 `_materialize_image()` 是个**同步函数**（`def`，不是 `async def`，内部用 `urllib` 下载），返回的是普通字符串。**对字符串做 `await` 必然抛 `TypeError`**，就是这个 `object str can't be used in 'await' expression`。

更坑的是它被外面一层 `except Exception` 接住了，于是异常变成了一句警告，然后降级成文生图。**没有任何一条路径会让用户意识到图生图坏了**。

坦白说：这行代码从写出来那天起就是错的，**图生图在此之前一直不可用**。之前几轮测试没覆盖到这条路径，是测试漏了，不是「偶发」。

#### 修复

```python
return await asyncio.to_thread(_materialize_image, first), None
```

为什么不干脆去掉 `await` 直接 `return _materialize_image(first), None`？因为那样虽然不报错了，但 `urllib` 下载是**阻塞 IO**：在协程里直接调用会把整个事件循环卡住，最坏情况（下载超时）**30 秒内机器人的其他消息一条都处理不了**。`asyncio.to_thread()` 把这次调用丢到线程池里执行，既不阻塞事件循环，也照常拿得到返回值。

顺便把 `_materialize_image` 的 docstring 改了 —— 原来写的是「因为调用点在做二次确认挂起时需要立刻拿到结果」，这个理由站不住脚（`to_thread` 一样能立刻拿到结果），现在改成明确说明「这是同步函数，调用方请用 `asyncio.to_thread` 包一层」。

**回归护栏**：测试里加了一条，扫描 `main.py` 的可执行代码行，一旦再出现 `await _materialize_image(...)` 就报错；同时断言 `_materialize_image` 是同步函数、`asyncio.to_thread(_materialize_image` 存在。

---

### Bug 2：QQ 发图超时重试 → 收到两张一样的图

#### 问题

用户日志里反复出现：

```
[WARN] 发送图片失败（第 1/2 次）: Timeout: NTEvent ...sendMsg...
```

然后用户在群里收到了**两张一模一样的图**。

#### 原因

v1.4.3 加的发图重试（失败后隔 2 秒再发一次）基于一个假设：**超时 = 没发出去**。

这个假设对很多网络请求成立，但对 NapCat 发大图**不成立**。`sendMsg Timeout`（retcode=1200）很多时候是图片**已经成功发出去了**、只是 QQ 客户端那边回执慢，NapCat 等到超时就报了错。这时第 2 次重试就变成了实打实的重复发送。

#### 为什么之前选「重试」，这次反过来

这是「宁可重发」和「宁可漏图」之间的取舍：

- **重试**：偶尔漏图时能救回来，代价是偶尔多一张重复的图
- **不重试**：绝不会重复，代价是偶尔图发不出去（但图已经生成好了、磁盘上有本地路径，用户可以自己去取）

插件**没法从超时本身判断图到底送没送出去**，所以不该替用户拍板。

用户实测反馈是：重复发图带来的困扰 > 偶尔漏图（漏图时提示里给了本地路径，可以补救）。所以 **v1.4.5 起默认改为「不重试」**，同时保留开关给需要的人。

#### 修复

1. 新增配置项 **`send_retry_on_timeout`（发图超时自动重试）**，默认 **关闭**
   - **关闭**（默认）：只发一次，超时就提示「图已生成但发送超时」并给出本地路径。不会重复，但偶尔漏图
   - **开启**：超时后隔 2 秒再发一次。宁可重发也不丢图，但可能收到两张重复的图
2. 关闭重试时，日志文案从 `发送图片失败（第 1/2 次）` 改成 `发送图片失败: ...`（没有第 2 次了，写「1/2」只会误导）
3. 关闭重试时，失败提示末尾追加一句引导：「如果经常这样，可以在插件配置里打开『发图超时自动重试』（但可能出现重复发图）」
4. 这个开关**只出现在 AstrBot 原生插件配置页**，没加进 WebUI 面板 —— 面板目前只做画师串 / 负向词 / 拼接预览 / 预设管理，不展示任何开关类配置，为一个开关单独造一套开关组件不值当

---

### 测试

- `tests/test_prompt_dict_quality.py` **7960/7960 通过**（与 v1.4.4 一致，本次未动词库）
- `tests/test_webui_api.py` 140 → **163 项全过**：
  - **Bug 1（参考图落地）**：假 `quoted_message` 模块喂 `base64://<1x1 PNG>` → 断言返回 `(路径, None)`、文件真实存在且非空、内容与解码结果一致，用完即删；`data:` URL 同样落地；本地路径原样返回；空列表 → `(None, None)` 不报错；认不出的格式 → 给错误提示不抛异常；读取被回复消息抛异常 → 兜住并带出原因；回归护栏 4 条（同步函数 / 协程函数 / 无 `await _materialize_image` 代码行 / 有 `asyncio.to_thread`）
  - **Bug 2（发图重试开关）**：开关关闭时 `_FakeEvent` 首次抛错 → 只尝试发 1 次图且返回文字说明（含本地路径 + 引导文案）；开关关闭时首次成功 → 恰好 1 次；开关关闭时「第 1 次失败、第 2 本可成功」也照样放弃（不重复）；开关开启时第 1 次失败第 2 次成功 → 发 2 次且正常收到图；全部失败 → 仍返回「图已生成」说明；重试开启时不出现「去打开重试」的引导

## v1.4.4

直译管线两处改进：**单角色自动补 `solo`**、**连接词不再让整句交给模型**。起因是一条线上日志。

### 问题

用户发 `#nai 原神神里绫华穿着泳衣在沙滩玩耍`，直译结果是：

```
kamisato_ayaka_(genshin_impact), swimsuit, beach, playing
```

两个毛病：

1. **没有人数标签**。`1girl` / `solo` 是 NovelAI 最重要的主体标签——没有它，模型得自己猜画面里是一个人还是几个人，主体经常跑偏。
2. **这句本来一个字都不用给模型翻**。`原神` `神里绫华` `泳衣` `沙滩` 全在词库里，`玩耍` 也该在。却因为夹着 `穿着` 和 `在` 两个虚词，整句被丢给了模型——白等一次网络，模型还把 1girl 弄丢了。

### 原因

**人数标签丢失**：系统提示词第 7 条写的是 "start with 1girl or 1boy **when applicable**"，太软，模型直接省掉。更糟的是示例自己就在打脸——`战双 比安卡 泳装` 和 `鸣潮 达妮娅 泳装` 两条示例的 Output 都没带 1girl，规则和示例矛盾时模型听示例的。

**整句交给模型**：词库的子串兜底有一条铁律「替换后还有中文残留就整段放弃」。这条规则是对的——它防的是 `超级赛亚人发型` 被替成 `超级赛 ajin 发型` 这种半截污染。但它把 `穿着` `在` 这类**对画面毫无贡献的虚词**也当成了「没理解的内容」。

### 修复

**A. 系统提示词硬化**

- 第 7 条改成硬约束：单人画面**必须**以 `1girl` / `1boy` 开头（性别不明用 `solo`），只有用户明确要多人或无人时才不加
- 修掉示例与规则的矛盾：比安卡 / 达妮娅两条示例补上 `1girl`；`原神 雷电将军` 示例的 `1girl` 从末尾挪到最前
- 赛博朋克城市示例的 Output 里原来有个 `1girl`，可输入压根没提人——这直接违反第 11 条「不许脑补」，删了
- `_conf_schema.json` 里 `translate_system_prompt` 的默认值和代码里的 `SYSTEM_PROMPT` 必须**逐字节一致**（用户没改过配置时读的是 schema 那份），两边同步改了，测试里加了一致性断言

**B. 代码层兜底：单角色自动补 `solo`**

模型不听话是常态，提示词改了也不能全指望它。新增 `_ensure_subject_count()`，在翻译结果收尾时检查：

- 已经有任何人数标签（`1girl` / `2girls` / `solo` / `no humans` …，带 `{{}}` / `1.2::` 权重语法也认）→ 不碰
- 否则数「角色标签」有几个：命中词库角色名，或者长得像 `xxx_(作品名)` 消歧写法
- **恰好 1 个** → 在最前面插 `solo`；0 个或 ≥2 个都不动

**为什么补 `solo` 而不是 `1girl`**：词库只存了角色名，**没有性别**——代码不知道 `kamisato ayaka` 是女的。猜错性别比不写更糟：`1girl` 配一个男角色会让模型在两者间摇摆。`solo` 只说「就一个人」，不分性别，怎么都不会翻车。

**为什么多角色不补**：两个人可能是 `2girls`，也可能是 `1girl, 1boy`，人数和性别都不确定，宁缺勿错。

为此 `PromptDictionary` 新增 `character_tags` 属性：从 `characters.json` 提取角色名（658 条），**剔除作品名**（`genshin impact` / `wuthering waves` 这些）。作品名判定：值以 `(series)` 结尾、或出现在别的值的消歧括号里（`wuthering waves` 出现在 `denia_(wuthering_waves)` 里 → 是作品）、或在显式补充列表里（原神这类角色全是裸名、括号派生不出来的）。不剔除的话 `原神 神里绫华` 会被数成两个角色，该补的时候不补。

顺带修了一个隐性问题：以前「词库全命中」分支直接 `return merged`，**跳过了近义词折叠和质量词加权**——词库命中越全，处理反而越少。现在两条返回路径统一走 `_finalize()`。

**C. 连接词不再阻断词库直译**

子串兜底放宽一档：替换后的残留如果**全部是连接词**（`穿着 戴着 拿着 抱着 正在 一个 一位 一名 穿 戴 在 的 和 与 着 了`），删掉后一个汉字都不剩 → 接受为命中。

**为什么只在「残留全是连接词」时才剥**：这批词的共同点是「删掉它画面不会有任何变化」，所以剥掉不会产生用户没写过的内容。但只要残留里还有**一个**非连接词的汉字（`超级赛`、`发型`），就说明这段里有我们没理解的东西，仍然整段放弃——「半截命中比不命中更危险」这条原则没变。`超级赛亚人发型` 的反例写进了测试锁死。

**D. 词库加词**

`scene.json/action` 新增 `玩耍` `嬉戏` → `playing`。没加单字 `玩`——子串模式下会误伤 `玩偶`。词库 1576 → 1578 条。

### 效果

```
原神神里绫华穿着泳衣在沙滩玩耍
→ solo, genshin impact, kamisato ayaka, swimsuit, beach, playing    （词库全命中，未调模型）
```

### 没改的

- 没有给词库加性别字段来补 `1girl`。角色 700 多条，人工标注成本高、维护容易漏，而 `solo` 在效果上已经够用
- 连接词列表**故意很短**，只放纯功能词。形容词 / 名词一律不放——那些词有画面含义，剥掉就是丢信息
- `substring` 匹配模式（整句子串替换）的行为没动，这次只改 `segment` 模式的兜底分支

### 测试

- `tests/test_prompt_dict_quality.py` 7905 → **7960 项**：主例句全命中且无中文残留；只含连接词 + 词库词的 4 种组合全命中；`超级赛亚人发型` 反例仍进 misses；`character_tags` 含 8 个角色、不含 17 个作品名 / 非角色词；新增词条可查、单字 `玩` 未收录
- `tests/test_webui_api.py` 101 → **140 项**：`_ensure_subject_count` 纯函数 22 条（已有标签不动 / 权重语法识别 / 单角色补 solo / 作品名不计数 / `fate_(series)` + saber / 双角色不动 / artist 不算 / `(character)` 不算）；`translate()` 词库全命中路径端到端 3 句；`dictionary=None` 容错；系统提示词硬化 6 条 + schema 一致性

## v1.4.3

修一个**误报**：QQ 端发图超时被记成了「生图失败」。

### 问题

用户生图时日志报：

```
[Nai2API] 生图失败: <ActionFailed status='failed', retcode=1200, ...
message='Timeout: NTEvent serviceAndMethod:NodeIKernelMsgService/sendMsg ...'>
```

看着像 NovelAI 挂了，实际不是。`NodeIKernelMsgService/sendMsg` 是 **QQ 客户端（NapCat / NTQQ）发消息的内部接口**，`retcode=1200` 是 NapCat 的「发送超时」。也就是说：**图已经画好、点数已经扣了、文件已经存到本地了**，是最后一步「把图发进 QQ」时 QQ 那边没在规定时间内回应——发大图（尤其 2K / 4K）时偶发。

之前 `_run_generate_command` 把「调 NovelAI 生图」和「发图到聊天」包在同一个 `try` 里，所以发图的异常也被当成了生图失败，日志和用户提示都指向了错误的方向。

### 修复

- **生图和发图拆成两个 try**：生图失败仍报「生图失败」；发图失败报「图片已生成，但发送超时」，并附上本地文件路径，说明点数已消耗、NovelAI 是正常的
- **发图自动重试**：失败后隔 2 秒再发一次（共 2 次）。NapCat 这类超时多数是瞬时的，第二次通常就成功了
- **信息标签发送失败不再影响主流程**：图已到、标签没到只记日志
- **LLM 工具路径同样处理**：`nai_generate` 发图失败时明确告诉模型「图已生成、不要重试生图（会重复扣点）」——以前模型看到「生图失败」可能会再调一次，白扣一次点
- 新增 `ImageSendError` 异常类型 + `_short_err()`：把 NapCat 那一大段带换行的事件名压成一行可读文本，不再原样甩给用户

### 没改的

- 没有加图片压缩 / 转格式。这个超时是 QQ 客户端响应慢，不是图太大发不出去；如果后续确认是尺寸问题再考虑
- 重试次数 / 间隔是类常量 `_SEND_RETRIES` / `_SEND_RETRY_DELAY`，暂不暴露到配置（没必要让新手多一个不知道怎么填的选项）

### 测试

- `tests/test_webui_api.py` 92 → **101 项断言**：模拟 NapCat `ActionFailed`，验证「失败一次后重试成功不出现失败文案」「全部失败返回图已生成说明 + 本地路径」「真正的生图失败仍走老提示」「`_short_err` 压缩效果」

## v1.4.2

预设从「只有一条画师串」扩成**三段式**：画师串 + 附带正向词 + 附带负向词。起因是用户在 WebUI 里新增预设时发现只能填画师串——想让某个预设顺带压掉某些东西（比如 2.5D 风压 `green`）、或者常驻补几个构图词，以前只能硬塞进画师串里。

### 新增：预设可以附带正向词 / 负向词

| 段 | 拼到哪 | 说明 |
|----|--------|------|
| **画师串** | 整条提示词**最前面** | 和以前一样，替代全局画师串。**现在允许留空**（沿用全局画师串） |
| **附带正向词** | 用户提示词**后面** | 每次用这个预设生图都自动带上，适合放构图、光照、场景氛围。和用户提示词重复的标签自动跳过 |
| **附带负向词** | 全局负向词**后面** | 是**追加**不是替换：全局的 `bad hands` / `lowres` 兜底仍然生效 |

三段任意一段非空即可保存。所以现在可以做「夜景」这种只加 `night, city lights` + 压 `daylight`、不动画风的预设。

**为什么正向词放用户提示词后面而不是前面**：NovelAI 越靠前权重越高。画师串（风格）理应最前，用户描述的画面其次，预设的「常驻补充」放最后，这样用户写的内容不会被预设里的固定词压住。

**为什么负向词是追加不是替换**：全局负向词通常是通用兜底，预设的负向词是针对某种画风的额外约束。替换会把兜底丢掉，新手容易踩坑；想彻底换的把全局那份清空就行。

### 生效范围

- `/nai -p 预设名 ...` 指令、LLM 工具 `nai_generate(preset=...)`：都会把预设的正向 / 负向词并进去
- 预设正向词在**直译之后**拼入：它本来就是英文标签，不该再被直译模型改写；也在扣点确认之前拼入，确认提示里看到的就是最终内容
- WebUI 面板：新增 / 编辑预设弹窗多了两个框；预设卡片显示三段内容 + `+正向` `+负向` 小标记；**拼接预览新增「模拟预设」下拉**，选一个预设就能看到 `/nai -p` 之后最终发出去的是什么，每张预设卡片也有「预览」按钮直达
- 配置页 `custom_presets` 列表新增「附带正向词」「附带负向词」两栏
- `/nai presets` 列表会标注「（附带正向词 / 负向词）」，`/nai presets 名字` 详情分段显示

### 向后兼容

- 旧的 `presets.json` / 配置里只有 `artist` + `desc` 的预设，读进来自动补空的 `positive` / `negative`，行为和以前**完全一样**
- `/nai save 名字 画师串` / `/nai update` 指令不变（只操作画师串，想加正向 / 负向词去 WebUI）
- `PresetManager.save()` / `update()` 的新参数都是关键字可选参数，老调用方式不受影响
- 内置 5 个预设不带正向 / 负向词，`get_entry()` 统一补空串，调用方不用判断键存不存在

### 边界处理

- `_resolve_artist()`：预设存在但画师串为空 → 返回 `None`，让 client 退回全局画师串（以前返回空串会导致「画师串被清空」）
- `_apply_preset_extras()`：用户没给 `--negative` 时（`negative=None`）先取全局负向词再追加，否则 client 收到非 `None` 会当成「用户完全自定义」把全局那份丢掉
- 合并去重只做「整段完全相同（忽略大小写）」，`1.2::tag::` 和 `tag` 视为不同项——用户可能故意用不同权重

### 测试

- `tests/test_webui_api.py` 56 → **92 项断言**：三段式保存 / 回写 / 落盘、三段全空拒绝、`_resolve_artist` 空串退回、`_apply_preset_extras` 四种 negative 组合、预览带 preset、老格式 presets.json 兼容、`merge_tags` 边界、前端表单字段存在性
- 词库回归 7905/7905 通过

### 文件变化

- `core/preset_manager.py`：新增 `_normalize_entry` / `preset_has_content` / `merge_tags` / `get_entry` / `get_positive` / `get_negative`；`save` / `update` / `export_for_webui` / `parse_webui_presets` / `_load_file` / `_merge` 支持三段
- `main.py`：`_resolve_artist` 空串处理；新增 `_apply_preset_extras`，接入 `/nai` 指令和 `nai_generate` 工具；WebUI `presets` / `preview` 接口支持三段和 `preset` 参数；`/nai presets` 显示三段（新增 `_format_preset_detail`）
- `_conf_schema.json`：`custom_presets` 模板新增 `positive` / `negative` 两栏
- `pages/nai-config/{index.html,app.js,style.css}`：弹窗两个新框、卡片三段展示、预览「模拟预设」下拉
- `tests/test_webui_api.py`：+36 项断言

## v1.4.1

新增一个**独立的 WebUI 管理面板**，放在 AstrBot「插件」页 → 本插件详情页里。以前改画师串、负向词只能在配置页的小输入框里硬改，看不到最终拼出来是什么；现在有专门的页面了。

### 新增：NovelAI 配置管理面板

入口：AstrBot Dashboard → 插件 → `Nai2API 生图（nai_plus 增强版）` → 页面「NovelAI 配置管理」。

面板里有四块：

| 区块 | 能做什么 |
|------|---------|
| **画师串 / 质量前缀** | 大文本框编辑 `default_artist`，实时显示字符数 / 标签数、未保存提示；一键追加 NovelAI 官方质量词（`best quality` / `very aesthetic` / `masterpiece`…）；「重置为默认」 |
| **负向词** | 同上，编辑 `default_negative`，一键追加官方推荐负向词 |
| **拼接预览** | 输一段正向词，直接看到**最终发给 NovelAI 的正向 / 负向串**：画师串拼在哪、`-2::green::` 这类负权重挪去了哪、有没有自动补 `full body, standing`。用的是文本框里**当前**的内容，不用先保存 |
| **预设管理** | 自定义预设新增 / 编辑 / 删除；内置 5 个预设只读展示，但可以「复制为新预设」再改，或一键「设为默认画师串」 |

改完点保存**立即生效**，不用重载插件。主题跟随 AstrBot 的亮 / 暗色。

### 数据一致性（重要）

面板、`/nai save / del / update` 指令、配置页的「预设管理」列表，三个入口操作的是**同一份数据**：

- 画师串 / 负向词写进插件自己的配置文件（`data/config/astrbot_plugin_nai_plus_config.json`），和配置页改的是同一个键
- 预设全部走 `PresetManager` → `_persist_presets_to_config()` 回写，和指令走同一条路
- 预设名的校验规则（不能有空格、不能和内置重名、画师串不能为空）和启动时的 `parse_webui_presets()` 完全一致——不然这边存进去、重载时那边又静默丢掉，用户会一头雾水

### 踩坑记录：`context.get_config()` 不是插件配置

第一版后端用了 `self.context.get_config().save_config_async({...})`，看起来很合理，实际上 `Context.get_config()` 返回的是 **AstrBot 全局配置**（`data/cmd_config.json`，装的是 dashboard 密码、平台适配器那些）。往里写 `default_artist` 只会污染全局配置文件，插件下次启动根本读不到。

正确做法：构造函数传进来的 `config` 本身就是一个 `AstrBotConfig` 对象，绑定的是插件自己的配置文件，直接 `self.config.save_config_async({...})` 就对了。

### 修复：`/nai save` 加的预设重载后消失（老 bug，被新测试抓出来）

顺着上面这个坑往下查，发现 v1.3.1 加的 `_persist_presets_to_config()`（指令改完预设回写配置）用的也是 `context.get_config()`。全局配置里没有 `custom_presets` 这个键，函数里 `if "custom_presets" not in cfg: return` 永远成立——**它从上线起就一次盘都没写过**，只是静默 return，日志级别还是 debug 所以没人发现。

表现：`/nai save 名字 画师串` 当时能用，重载插件后预设没了（配置页的旧列表把 presets.json 盖掉了）。现在改成写 `self.config`，新增的 `tests/test_webui_api.py` 里有断言锁定「保存预设后插件配置里必须有它」。

### 版本要求变更

- `astrbot_version` 从 `>=4.16.0` 升到 **`>=4.26.0`**。面板后端用了 `astrbot.api.web`（`json_response` / `error_response` / `request`），这个模块是 AstrBot 4.26.0 才有的
- 老版本 AstrBot 装了也不会炸：`_register_webui_apis()` 里 `ImportError` 直接跳过并打一行日志，只是没有面板，`/nai` 指令一切照旧

### 仓库卫生：移除误提交的 `data/` 目录

原仓库把 `data/cmd_config.json`（**AstrBot 全局配置**，里面有 dashboard 的密码哈希）和 `data/t2i_templates/*.html`（AstrBot 自带的文转图模板）一起提交进来了。这两样都不是插件的东西，插件代码一处都没引用；密码哈希放在公开仓库里更是不该。

这次从 git 里移除（`git rm --cached`，本地文件不动），并把 `data/` 加进 `.gitignore`。
**如果那个哈希对应的是你正在用的 dashboard 密码，建议改一次密码**——文件虽然删了，git 历史里还能翻到。

### 文件变化

- 新增 `pages/nai-config/{index.html,app.js,style.css}` —— 面板前端（原生 JS，无构建步骤）
- 新增 `.astrbot-plugin/i18n/{zh-CN,en-US}.json` —— 页面标题 / 描述的多语言
- 新增 `tests/test_webui_api.py` —— 面板后端回归测试（56 项断言，stub 掉 AstrBot 离线可跑），重点锁「配置写对文件、不污染全局配置、老版本 AstrBot 不炸」
- `main.py` 新增 `_register_webui_apis()`，注册 5 个接口：`config`(GET)、`config/artist`、`config/negative`、`presets`、`preview`(POST)


## v1.4.0

对照 NovelAI 官方文档和 Danbooru 官方标签，把内置词库和切词逻辑过了一遍。修了 3 个「画出来不对」的隐性 bug，顺手把英文输入的处理补齐。

### 修复：3 个角色名张冠李戴（明日方舟）

| 中文 | 以前翻成 | 实际是谁 | 现在翻成 |
|------|---------|---------|---------|
| 艾雅法拉 | `ifrit_(arknights)` | ifrit 是**伊芙利特** | `eyjafjalla_(arknights)` |
| 焰尾 | `nearl_(arknights)` | nearl 是**临光** | `flametail_(arknights)` |
| 黑键 | `mudrock_(arknights)` | mudrock 是**泥岩** | `ebenholz_(arknights)` |

这种错最难查：日志里显示「词库命中」，一切正常，就是画出来是另一个人。三个被顶掉的角色（伊芙利特 / 临光 / 泥岩）也补进词库了。

### 修复：同一个中文键在两处各写一个值

词库分三个文件加载，**同一个键后加载的会静默覆盖前面的**，没有任何提示。扫出 4 处冲突：

- `露西亚`：绝区零组写 `lucia`，战双组写 `lucia_(pgr)` → 运行时永远是战双那个，绝区零那条从上线起就没生效过。绝区零 2.3 那位角色官方中文名是**卢西娅·艾洛温**（Lucia Elowen），所以改成独立的键 `卢西娅` → `lucia elowen`；`露西亚` 保留给战双
- `正面` / `背景虚化` / `广角镜头`：外观文件和场景文件各写了一个不同的英文 → 删掉场景文件那份，只留一份

新增回归测试 `tests/test_prompt_dict_quality.py`，其中一条断言就是「**值冲突的重复键必须为 0**」——以后再有人加词条撞了键，测试直接红。

### 修复：词库里的纯英文键从来没命中过

词库里有 20 多条纯英文键：`2b`、`dva`、`saber`、`HK416`、`MEIKO`、`jks`……它们存在的意义是把用户随手打的简写补全成完整消歧标签（`2b` → `2b (nier:automata)`）。

但切词逻辑里有一句「片段不含中文 → 直接放行不查表」。于是这些键**一次都没生效过**。用户打 `2b`，模型只拿到孤零零的 `2b`，经常画成别的。

现在纯英文片段也会先查一次表，查到就替换，查不到才原样保留。顺带做了大小写兜底：词库里写的是 `HK416`，用户打 `hk416` 也能命中。

### 新增：英文多词标签不再被切碎

以前 `1girl, long hair, blue eyes` 会被按空格切成 `1girl, long, hair, blue, eyes`——5 个碎片，「长发」「蓝眼」两个概念直接没了。

原因是切词把空格当分隔符，那是给中文输入设计的（「白发 双马尾 微笑」要能按空格切开）。现在改成两层：先按逗号切成大项，**纯英文大项整体保留不再按空格切**，中文大项照旧按空格切。中英混排（`白发 white hair`）按空格切，英文碎片原样保留——想让英文标签完整就用逗号隔开，这也是 NovelAI 的规范写法。

### 统一：括号消歧标签的写法

蔚蓝档案组的 `arona_(blue archive)` 是下划线和空格混写——Danbooru 认 `arona_(blue_archive)`，NovelAI 官方推荐 `arona (blue archive)`，这样两边不靠。改成 NovelAI 推荐的空格写法。

补充说明：NovelAI 官方文档明确 V4 及以上模型**建议用空格不用下划线**（`white hair` 而不是 `white_hair`）。词库里现存的下划线写法（`amiya_(arknights)`）是 Danbooru 原生格式，NovelAI 一样认——这次**没有**一刀切改掉，两种写法效果等价，改了只是制造大量无意义 diff。

### 其他

- 词库计数：1576 条（角色 830 / 外观 469 / 场景 277），去重后加载 1569 条
- 单机游戏组补了 `守望先锋` / `尼尔` / `最终幻想` 作品名标签
- `阿尔托莉雅` 从明日方舟组挪走（本来就放错了，fate 组已有）
- 新增 `tests/test_prompt_dict_quality.py`：7905 项断言，覆盖格式 / 重复键 / 已知错译回归锁定 / 英文键命中 / 切词边界，不依赖 AstrBot 可直接跑

## v1.3.1

新增 **WebUI「预设管理」**。以前加自定义预设只有两条路：跟 AI 说话让它转述，或者手改 `presets.json` 再重启。现在在插件配置页里点几下就行。

### 用法

WebUI 插件配置 → 「预设管理」→ 点**添加条目** → 填三项：

| 字段 | 说明 |
|------|------|
| **预设名** | 调用时用的名字，不能含空格（含空格会让 `/nai -p 名字` 解析不出来，会被自动跳过并记日志）。不能和 5 个内置预设重名。 |
| **质量前缀 / 画师串** | 会拼在提示词最前面。写画师串、质量词、构图词都行。里面的负权重（`-2::green::`）会自动移到负面提示词。 |
| **备注** | 只用于在列表里显示，可留空。 |

改完**立即生效**，不用重启。每个条目的右上角有删除按钮。

> 预设名不能和内置预设重名（`2.5D唯美风` / `韩漫小清新风` / `本子动漫风` / `GalGame风` / `动漫风`）。重名的条目会被跳过并记日志——因为顶掉内置预设之后，`/nai -p 动漫风` 拿到的就不是文档里那个效果了，出问题很难排查。

### 两条路操作同一份数据

WebUI 编辑器和 `/nai save`、`/nai del`、`/nai update`、以及 LLM 工具（`nai_save_preset` / `nai_update_preset` / `nai_delete_preset`）**读写的是同一份预设**，不会打架：

- **以 WebUI 为准**：同名时 WebUI 里的值胜出
- **指令改完自动回写配置**：`/nai save` 加完预设，会把最新状态写回插件配置。否则下次重载时，配置里的旧值会把指令的改动覆盖掉——表现就是「我用指令加的预设过一阵自己没了」，很难排查
- **旧数据自动迁移**：如果 `presets.json` 里有 WebUI 里没有的预设（历史数据），启动时自动补进 WebUI 配置，并打日志告诉你迁移了哪些。只迁一次

### 为什么要迁就「不能静默丢数据」

启动时如果 `presets.json` 里有、WebUI 里没有的项，有两种可能：① 用户还没迁移的旧数据；② 用户在 WebUI 里删掉了它。**光看配置无法区分这两者。**

所以启动路径选了「迁进来」（宁可多留一次，用户能再删；静默丢数据是不可逆的），而重载路径提供 `exact=True` 模式走「以配置为准、不在配置里的就删」。这个区分写在 `PresetManager.reload_from_webui()` 的 docstring 里。

### 容错

配置写坏了的代价必须是「这个预设不生效」，不能是「插件加载失败」——后者会让用户连生图都用不了。所以：

- 该项不是对象 / 预设名为空 / 名字含空格 / 画师串为空 / 与内置重名 → 跳过该项并记日志，其余照常
- 整个配置项类型不对（不是列表）→ 当作空处理
- `presets.json` 不存在 / 是坏 JSON / 结构不是 dict → 当作空处理，插件正常加载
- 回写配置失败（拿不到 context / 配置项被删 / 写盘报错）→ 只记日志，不影响 `/nai save` 的返回

### 测试

新增 `test_webui_presets.py`，71 项断言，覆盖解析 / 双来源合并 / 回写格式 / 指令改完的同步 / 容错五部分。全量回归 18 个文件 781 项断言全绿。

### 为什么用 `template_list`

AstrBot 的配置项有 10 种合法 `type`，其中只有 `template_list` 能提供「列表 + 每项多字段 + 增删按钮」这种结构化编辑器。那些 `select_provider` / `select_personas` 之类的特殊下拉是**硬编码的内置集合，插件无法扩展**，所以只能用 `template_list`。

存储时每项会带一个 `__template_key` 标记（值为 `preset`），指向 `templates` 里定义的模板。这个字段是编辑器认模板用的，缺了会报「找不到对应模板，请删除后重新添加」。回写配置时已经带上了。

## v1.3.0

新增**群聊黑名单**。被拉黑的群，生图功能彻底哑火，而且机器人不会在该群留下任何痕迹。

### 用法

WebUI 插件配置里找到「群聊黑名单」，填群号，多个用逗号或换行分隔：

```
123456789
987654321
```

留空 = 不启用，所有群照常。**只对群聊生效，私聊不受影响**（黑名单是「群黑名单」，
不是「用户黑名单」）。

配置解析很宽容，下面这些写法都认：

| 写法 | 说明 |
|---|---|
| `123, 456` | 逗号（中英文都行） |
| 换行分隔 | 一行一个群号 |
| `123；456` | 分号 / 顿号 / 竖线 / 空格 |
| `群号:123` | 带常见前缀，自动剥掉 |
| `aiocqhttp:123` | 多平台场景，只取群号部分 |

解析不出来的项会被**静默跳过**，不会让插件加载失败 —— 配置写错最多是黑名单不生效，
但如果插件挂掉，用户连生图都用不了了。

### 三层拦截

需求是「让 astrbot 的 llm 也直接无视掉该生图指令」，这句话拆开其实是两个要求：
**别生图** + **别回复**。实现上做了三层，因为单靠任何一层都有漏网的可能：

| 层 | 拦截点 | 行为 |
|---|---|---|
| 第 1 层 | `#nai` 命令入口 | 整个命令链路静默丢弃，**连 `presets` / `balance` 等子命令也一样** |
| 第 2 层 | 6 个 `nai_*` LLM 工具入口 | 返回一句「本群未启用此功能」，让模型收手、不再重试 |
| 第 3 层 | `filter.on_llm_request` 钩子 | **直接掐断 LLM 请求**，模型根本收不到这条消息 |

### 第 3 层为什么必须用 `on_llm_request`

这是我一开始想错的地方，记下来免得以后又走回头路。

最直觉的做法是「把工具的 `active` 设成 `False`」—— AstrBot 确实有 `active` 字段，
`get_light_tool_set()` / `get_param_only_tool_set()` 也会过滤它。**但主链路不走这俩方法。**

AstrBot 4.28.0 里，工具 schema 的序列化走的是 `ToolSet.openai_schema()`，
而它**压根没有 `active` 过滤**，照单全收：

```python
def openai_schema(self, omit_empty_parameter_field: bool = False) -> list[dict]:
    result = []
    for tool in sorted(self.tools, key=lambda tool: tool.name):
        func_def = {"type": "function", "function": {"name": tool.name}}
        ...
        result.append(func_def)
    return result
```

调用方还用了 `if tool_list:` 这种写法（非空列表恒为真）：

```python
tool_list = tools.get_func_desc_openai_style(...)
if tool_list:
    payloads["tools"] = tool_list      # ← active=False 的工具照样在这里面
```

所以 `active=False` 只会让工具在 UI 上显示成禁用，**该发给模型还是发**。
模型能看见 `nai_generate`，就会去调。

另外 `llm_tools` 是**全局注册表**（`astrbot.core.provider.register.llm_tools`），
不是按群隔离的，所以也没法「只给这个群摘掉工具」—— 摘了会影响所有群。

真正管用的是 `on_llm_request`：它在请求构造完之后、真正发给模型之前触发，
此时 `event.stop_event()` 会让 pipeline 直接放弃这次 LLM 调用
（`internal.py`：`if await call_event_hook(...OnLLMRequestEvent, req): return`）。
模型收不到消息，自然不可能去调生图工具 —— 这才是「从源头掐断」。

### 需要知道的取舍

第 3 层一停，**这个群的 LLM 请求就整个没有了**，不只是生图。

这是「让 LLM 无视生图指令」的必然结果：要它无视，就得让它收不到。
如果你希望黑名单群保留正常的 AI 聊天、只是不能生图，那就别指望第 3 层 ——
1、2 两层已经能保证「生不出图」，第 3 层额外保证的是「LLM 压根不知道有人要生图」。

这个钩子只作用于**本插件**，不会动 AstrBot 的全局会话配置，
也不影响该群里其它插件的正常工作。

### 触发方式全覆盖

生图有两条**完全独立**的触发路径，黑名单必须两条都堵：

| 触发方式 | 路由 | 被哪层拦 |
|---|---|---|
| 唤醒词（`/nai`、`#nai`、`.`、`!`…） | `filter.command` 匹配 → 命令 handler | 第 1 层 |
| 改了命令名（`/niu`、`/绘`…） | 别名走同一主流程 | 第 1 层 |
| **@ 机器人** / @全体成员 / 引用机器人 | 不匹配任何命令 → 直接进 LLM | 第 2 + 3 层 |
| 群里说「画一个 xxx」 | 唤醒后进 LLM | 第 2 + 3 层 |

**换唤醒词 / 改命令名为什么不会绕过**：拦截点比「唤醒词解析」更靠前 —— AstrBot 的
`filter.command` 在进入 handler 之前就已经把唤醒前缀（`/`、`#`、`.`、`!`）和命令名
一起剥掉了，handler 收到的 `args` 是「命令之后的文本」。所以黑名单判断放在 handler
入口，就天然覆盖所有唤醒词和所有命令名。`command_names` 配的别名也走同一主流程。

**@ 为什么靠第 2、3 层**：@ 在 `waking_check` 阶段只是被标记
`is_at_or_wake_command = True`（`wake_prefix` 是空串），**不匹配任何命令**，
直接落到 ProcessStage 的「被 @ 或者通过唤醒前缀」分支 → `agent_sub_stage` → LLM。
这条路压根不经过命令 handler，第 1 层够不着，由 `on_llm_request`（第 3 层）
掐断 + 工具入口（第 2 层）兜底。

### 静默的具体含义

- 第 1 层：不发任何消息。回任何话都会暴露「机器人在这里」，而你要的是「直接无视」
- 第 2 层：给模型的回复是**回给模型的**，不是发到群里的，用户看不见
- 第 3 层：只停止事件，不产生任何输出

三层都不在群里发东西。群里看到的和机器人不存在时完全一样。

### 测试

新增 2 个测试文件、143 项断言（全量回归 614 项全绿）：

- `test_group_blacklist.py` —— 57 项，覆盖群号解析的各种格式、命中判断、容错
  （空值 / 垃圾输入 / `get_group_id` 缺失或抛异常都不能炸）、前缀相似不误判
- `test_group_blacklist_integration.py` —— 86 项，把插件**真的实例化**起来，
  逐层验证：命令静默丢弃、6 个工具全部拦截、钩子确实 `stop_event`，
  接线完整性（漏一个工具就能测出来），**命令名/唤醒词变体**
  （`/nai`、`/niu`、`/绘`、`/draw` 全覆盖），以及 **@ 机器人路径**
  （确认它靠第 2、3 层而非第 1 层拦住）

## v1.2.5

修「张张都是半身」的真正根因。这个问题上一版（v1.2.2）修了一半，这次修完。

### 背景：v1.2.2 修了什么，漏了什么

v1.2.2 修的是「画师串压根没拼进提示词」—— 那时画师串被当成独立请求参数
发送，整串失效。用户升级后**画风确实生效了**（质感、配色都对），但**依然
是半身**。说明还有第二个原因。

### 根因：画师串里没有任何「拍到哪」的约束

看用户日志里两串画师串的实际内容：

默认串（`default_artist`）：

    misaka_12003-gou, dino_(dinoartforame), wanke, liduke, year 2025,
    realistic, 4k, textless version, ... photo-style artwork ...,
    1.63::photorealistic::, 1.63::photo(medium)::, best quality, ...

`动漫风` 预设：

    artist collaboration, 0.70::artist:necomi ::, ... , year 2024, perspective

把它们按类别拆开：

| 类别 | 内容 |
|------|------|
| 画师 | `misaka_12003-gou`、`necomi`、`kanda done` 等 |
| 年份 | `year 2024` / `year 2025` |
| 画质 | `realistic`、`4k`、`photorealistic`、`photo(medium)` |
| 风格描述 | `lively color`、`graphic texture`、`realistic skin surface` |
| 质量词 | `best quality`、`absurdres`、`masterpiece` |
| **构图/景别** | **默认串：无；动漫风：只有 `perspective`** |

**画师串负责「什么质感什么画风」，不负责「拍到哪」。** 而用户 prompt 里
写的 `wuthering_waves, denia_(wuthering_waves), swimsuit` 是「谁 + 穿什么」，
「拍到哪」也是空的。

两处都没给构图约束时，NovelAI 会退回训练数据的统计偏好 —— 默认就是
`portrait` / `upper body`。**这就是张张都是半身的机制。**

### 修复一：内置预设补上构图标签

给 5 个内置预设各补一组贴合其风格调性的构图标签。注意不是统一塞
`full body` —— 每个风格的合适景别不同：

| 预设 | 追加的构图 | 为什么 |
|------|-----------|--------|
| `2.5D唯美风` | `cowboy shot, looking at viewer` | photo-style 取向，全身会削弱它的人像质感；七分身兼顾身材与面部细节 |
| `韩漫小清新风` | `full body, standing, looking at viewer` | 日常小清新，全身最贴 |
| `本子动漫风` | `cowboy shot, looking at viewer` | 倾向人物特写与身体表现 |
| `GalGame风` | `full body, standing` | 立绘风本来就该全身 |
| `动漫风` | `full body, standing` | 原本只有 `perspective`，补上景别 |

`DEFAULT_ARTIST`（用户不指定预设时用的）和 `_conf_schema.json` 里
`default_artist` 的默认值同步更新，三份保持一致 —— 否则用户换不换预设
行为会飘。

### 修复二：新增构图兜底（`auto_composition`，默认开启）

预设补了标签，但用户如果自己建了自定义预设（多半不会写构图），问题依旧。
所以再加一层兜底：**当提示词和画师串都没给构图约束时，自动补
`full body, standing`。**

判断逻辑在 `has_composition()`，识别这些关键词：

- 景别：`full body` / `upper body` / `cowboy shot` / `portrait` / `wide shot` / `close-up` / `face focus` …
- 视角：`from above` / `from behind` / `dutch angle` / `three quarter view` / `perspective` …
- 姿势：`standing` / `sitting` / `lying` / `looking at viewer` …
- 中文：`全身` / `半身` / `胸像` / `特写` / `立绘` / `构图` / `站姿` / `远景` / `俯视` …

**三条重要边界**（都有测试覆盖）：

1. **用户明确要半身时不插手** —— 提示词里写了 `upper body` / `半身` /
   `portrait`，兜底就不补。不会把「我就想画头像」强行改成全身。
2. **幂等** —— 补过的串再跑一次不会重复追加。
3. **可关闭** —— `auto_composition = false` 完全关掉这层。

### 修复三：画师串里的负权重移到负面提示词

排查时发现的另一个问题。Nai2API 官方预设把**负权重**写在了正向画师串里：

    2.5D唯美风:  ..., realistic, 4k, -2::green ::, textless version, ...
    本子动漫风:  ..., realistic, 4k, -2::green ::, ...
                 ..., 2::best quality, ..., -4::Muscle definition, abs::

`-2::green::` 的语义是「强烈抑制绿色」。写在正向串里有两个害处：

1. NAI 的正向 prompt 里出现负权重是畸形写法，会干扰同一串里其他标签的
   注意力分配；
2. 它是**无差别压制绿色** —— 画粉发角色没感觉，但画**绿发角色**
   （原神纳西妲、蓝档案的绿系角色等）时会把角色本身的发色/瞳色一起压掉，
   属于「预设和角色打架」。

修复：新增 `split_negative_weights()`，把 `-N::tag::` 从正向串里提取出来，
由调用方拼进 `negative`。提取**不改变权重数值**（`-2` 提取后仍是 `-2`），
且是幂等的 —— 用户自定义预设里如果也写了负权重，同样会被处理。

日志新增两行便于排查：

    [Nai2API] 画师串里的负权重已移入负面: -2::green::
    [Nai2API] 未检测到构图标签，已补默认景别: full body, standing

### 测试

新增 `test_composition.py`，**64 项断言**，覆盖：

- 负权重提取（含「正权重 `1.35::` 不被误提取」「空串」「纯负权重串」边界）
- 构图检测（英文/中文关键词，含线上那条失败 prompt）
- 构图兜底（核心场景、用户要半身时不插手、幂等、空 prompt）
- 预设与默认串三份一致性
- 组合场景：完全复刻线上那两次真实调用

全量回归 **13 个测试文件、527 项断言，0 失败**。

### 升级后怎么验证

发一次 `#nai 鸣潮 达妮娅 泳装`，日志里应该能看到：

    [Nai2API] 画师串里的负权重已移入负面: -2::green::
    [Nai2API] 提示词: ..., cowboy shot, looking at viewer, wuthering_waves, denia_(...), swimsuit

重点是 `cowboy shot`（或 `full body`）出现在 prompt 末尾 —— 那就是景别
约束生效的标志。

如果想固定画头像，把 `auto_composition` 关掉，或直接在提示词里写
`半身` / `胸像` / `特写`（词库都能查到）。

## v1.2.4

这一版全部来自一次真实群聊的日志排查，修了 4 个问题、补了 3 类词。

### 修复：连写输入时词库完全用不上（严重）

用户打「鸣潮达妮娅」（没打空格），线上日志显示：

    提示词已直译: 鸣潮达妮娅 → dania_(wuthering_waves)
                              ^^^^^ 拼错了，正确是 denia_

词库里 `达妮娅` 明明有正确映射，却因为用户连写而完全没用上 ——
`split_segments()` 按 `[,，、;；\s]+` 切分，`鸣潮达妮娅` 是**一个整段**，
查不到就整体甩给模型，模型对冷门角色名经常拼错。

根因是 `apply_dictionary()` 在 `segment` 模式下**完全没有子串兜底**：
`replace_substring()` 那套逻辑只在 `substring` 模式下才会被调用，
而默认模式是 `segment`（配置项默认值），等于兜底形同虚设。

修复：逐词查不到时，对该片段再跑一次子串替换。

### 修复：子串兜底会「半截命中」污染 prompt（比不命中更危险）

加兜底的过程中立刻踩到一个坑。用户输入：

    一个婴儿，超级赛亚人发型，...

`超级赛亚人` 没收录，但词库里有 `亚人` → `ajin`，于是被替换成：

    一个婴儿, 超级赛 ajin 发型     ← 用户从没写过「ajin」

**半截命中比不命中危险得多**：不命中只是交给模型（顶多翻得一般），
半截命中是往 prompt 里塞用户根本没写过的内容，属于数据污染。

修复：兜底只在**能完整消化整个片段**时采用 —— 替换后若仍有中文残留，
判定为不可信，整个片段放弃兜底，原样交给模型。

### 修复：子串替换用空格分隔，NovelAI 认成混合概念

`replace_substring()` 原先把 `{en}` 用空格包裹。但 NovelAI 的标签分隔符
是**逗号**：空格分隔的 `wuthering_waves denia_(...)` 会被当成一个混合概念，
而不含空格的多词标签（`white hair`）也会和分隔用的空格混淆。

修复：改用逗号包裹（`, {en}, `），再由 `_tidy()` 收敛重复分隔符。

### 新增：剥离口语前缀

用户天然会说「画一个泳装爱蜜莉雅」「来张白发少女」。这些前缀对出图
没有任何作用，却会让整句查不到词库（词库里只有「泳装」「爱蜜莉雅」），
白白多调一次模型。新增 `strip_filler()`，在切分前剥离句首的
`帮我 / 请 / 来张 / 生成 / 画一个 / 给我 / 我想要 / 出图` 等填充词。

只在句首剥离（不碰句中），最多剥 4 层，剥空时返回原文。

### 词库：+75 条（1497 → 1572）

| 类别 | 新增 | 来自哪次线上问题 |
|------|------|-----------------|
| 复合服装表达 | +38 | 「裸体围裙」曾被拆成 `裸体, apron`（半对半错）；裸体围裙是 Danbooru 整体 tag `naked apron`。同时补了「只穿袜子」「国王的新衣」「赤足」及一批配饰/鞋靴/整体风格搭配 |
| 人名同音异写 | +10 | 用户打「爱蜜莉雅」，词库只有「艾米莉娅」—— 和 v1.2.3 的「泳装/泳衣」是同一类问题，只是发生在人名上。补了 Re:Zero 全员的常见译名变体 |
| 龙珠 & 画风 | +26 | 那次「超级赛亚人」的输入完全没有词条。补了龙珠主要角色、`超级赛亚人`/`赛亚人`、`气焰`/`金色气焰`，以及 `韩漫`/`日漫`/`美漫` |
| 其他 | +1 | —— |

分组统计：角色名 797 → **823**，外观 431 → **469**，合计去重加载 **1572** 条。

### 测试

- 单元测试 139 → **159** 项，新增第十三节「线上日志回归」，把这次日志里
  出现的每一条真实输入都固化成了断言（连写、半截命中防护、复合服装、
  口语前缀、人名异写、逗号分隔）
- 覆盖度评测新增 3 条线上用例，全部稳定绕过模型
- **完全绕过模型：13/19（68%）→ 17/22（77%）**
- **词条级命中率：84% → 88%**

## v1.2.3

修一个「中文残留 + 重复翻译」的 bug，并补上服装类近义变体。

### 修复：未命中片段同时留在 merged 和 remaining

`apply_dictionary()` 在某个片段没查到时，既把它塞进 `out_parts`（于是进了
「已翻译结果」`merged`），又把它放进 `misses`（于是通过 `remaining` 又发给
模型翻一遍）。同一段中文被处理了两次。

线上日志里这个 bug 长这样：

    [Nai2API] 提示词已直译: 鸣潮 达妮娅 泳装
      → wuthering_waves, denia_(wuthering_waves), 泳装, swimsuit
                                              ^^^^ 中文残留

最后的 `泳装` 就是残留 —— 前面 `wuthering_waves, denia_(...)` 是词库命中
的结果，漏出来的 `泳装` 原样留在 `merged` 里，模型翻出来的 `swimsuit` 又
接在后面，于是 prompt 里同时出现中文和它的英文译文。

中文标签不仅无意义，还会占用 token 预算、干扰 NAI 的注意力分配，属于纯
负面内容。

修复：未命中的片段**不再** append 进 `out_parts`，只进 `misses`，交给模型
处理。这样 `merged` 里保证只有已确认的英文标签，`remaining` 里只有待翻译
的中文，两者互不重叠。

修复后同一句输入：

    鸣潮 达妮娅 泳装 → wuthering_waves, denia_(wuthering_waves), swimsuit
                       （三大片段全部命中词库，模型零调用）

### 修复：substring 替换后标签粘连

`replace_substring()` 把中文直接替换成英文，两边没有留空格。当输入是没分隔
符的连写（如「白发双马尾」）时，替换后会和相邻片段粘成一个词：

    白发双马尾 微笑 → white_hairtwintails smile    ← 粘连，NAI 认不出

修复：替换时两侧各补一个空格，最后统一 `re.sub(r"\s+", " ")` 收敛空白。

### 修复：分组级 `_comment` 被当成词条加载

`_load_json_dict()` 只在最外层跳过 `_` 开头的键，分组内部的 `_comment`
说明文本会被当成真实词条塞进词库。修复后在嵌套循环里也做同样的跳过判断。

### 扩充：服装类近义变体（99 → 130 条）

排查中发现一个隐蔽问题：`泳衣` 在词库里，但用户更常打的是 `泳装`——后者
**不在**。这类「同一个概念有好几种中文叫法，词库只收了一种」的缺口会让
本该零调用的词白白漏给模型，而模型翻的时候还容易顺带堆同义词。

本次系统排查了 7 个服装子类，把缺的近义写法补齐：

| 概念 | 补齐的写法 |
|------|-----------|
| 泳装 | 泳装 / 泳衣 / 游泳衣 / 比基尼泳装 → `swimsuit` |
| 学校泳装 | 死库水 / 学校泳装 → `school swimsuit` |
| 制服 | 制服 / 学生装 / 校服 → `school uniform` |
| 内衣 | 内衣 / 胸罩 / 文胸 / 乳罩 → `bra` |
| 内裤 | 内裤 / 胖次 / 短裤衩 → `panties` |
| 连裤袜 | 连裤袜 / 裤袜 / 丝袜 → `pantyhose` |
| 过膝袜 | 过膝袜 / 长筒袜 / 大腿袜 → `thighhighs` |

排查后 7 个类别缺口清零。

### 修复：两个版权标签拼写错误

顺手做的全量扫描揪出两处标签畸形，都会让对应 tag 在 NAI 端失效：

| 错误写法 | 正确写法 | 说明 |
|---------|---------|------|
| `punishing:_gray_raven` | `punishing:gray_raven` | 冒号后莫名多了个下划线 |
| `honkai: star rail` | `honkai:_star_rail` | 标签内带空格 |

第一处影响 `战双` / `战双帕弥什` 两个键，第二处影响 `崩铁` /
`崩坏星穹铁道` / `星穹铁道` 三个键。注意 Danbooru 标签**内部允许冒号**
（`himeko_(honkai:_star_rail)`、`rem_(re:zero)` 都是合法官方写法），
所以不能笼统禁止冒号，必须精确到具体畸形模式。

另外确认了 PGR 系列角色标签已全部使用新的 `(pgr)` 后缀 —— Danbooru
此前把 `bianca(punishing:gray_raven)` 整体 alias 到了 `bianca(pgr)`，
本词库跟的是新名。

### 其他

- 词库总量 1466 → **1508 条**（去重后加载 1497 条），README 计数同步更新。
- 单元测试 139 项、全量回归全部通过；其中 3 条断言原本写的是旧
  （有 bug 的）行为，已按修正后的契约重写。
- 覆盖度评测新增 `鸣潮 达妮娅 泳装`、`战双 比安卡 泳装` 两个用例，
  当前 19 例中 13 例（68%）完全绕过模型，词元级命中率 84%。

## v1.2.2

修两个让「画风完全不生效」的 bug，并加上质量词自动加权。

### 修复：画师串压根没进提示词（严重）

**这是画风不生效、什么图都是上半身的直接原因。**

`final_artist` 被单独作为一个 `artist` 请求参数发出去，从来没拼进 `tag`
（正向提示词主体）：

    params = {
        "tag": prompt.strip(),          # ← 只有翻译结果
        ...
    }
    if final_artist:
        params["artist"] = final_artist  # ← 画师串孤零零待在这

而预设里写的其实是**正向提示词内容**：

    "动漫风": "artist collaboration, 0.70::artist:necomi ::, ..., year 2024, perspective"
    DEFAULT_ARTIST = "... best quality, absurdres, very aesthetic, masterpiece, ..."

`artist collaboration`、`year 2024`、`perspective`、`masterpiece` 这些全是
prompt 标签，被塞进独立字段后**全部失效**。后果：
- 画风标签没生效 → 画出来的不是预设风格
- 构图标签（`perspective` 等）没生效 → 缺少构图约束，NAI 退回训练数据的
  统计偏好，**默认出 portrait / upper body，所以张张都是上半身**

修复：把画师串拼进 `tag`，画师串在前（全局风格）、用户提示词在后：

    final_prompt = f"{final_artist.strip()}, {prompt.strip()}"

同时日志新增一行 `[Nai2API] 画师串: ...`，以后这类问题一眼可见。

### 新增：质量词自动加权

按需给质量标签套权重，强化整体画面精度：

- 命中 `best quality` / `masterpiece` / `absurdres` / `very aesthetic` /
  `detailed` 等质量词 → 自动包成 `1.2::best quality::`
- **只处理裸质量词**：已带 `1.3::xxx::`、`{{xxx}}`、`[xxx]` 的**原样跳过**，
  不覆盖用户意图，也不会产生 `1.2::1.3::xxx::::` 这种嵌套垃圾
- **内容标签不加权重**：给 `swimsuit` 这类加权重会让该概念过拟合、挤掉画面
  其他部分（正是之前画不对的原因之一）；质量词加权重是纯收益
- 两个配置项：`translate_quality_weight`（开关，默认开）、
  `translate_quality_weight_value`（倍数，默认 `1.2`）

### 其他

- 场景词库补 30 条构图/视角词（`上半身`/`全身构图`/`立绘`/`荷兰角`/`鸟瞰`
  等）—— 构图类是「全是上半身」的直接解药
- 词库 1440 → 1466 条；测试 110 → 124 项

## v1.2.1

修一批「翻译看着没问题、画出来却不对」的问题。根因不是翻译错了，是**模型多说了话**。

### 修复：模型瞎猜角色外观（最主要）

词库里没有的角色名会被整句丢给模型，模型不认识就**编**。真实案例：

    输入: 战双 比安卡 泳装          ← 用户只说了 3 个词
    输出: 1girl, bianca_(punishing:_gray_raven), swimsuit, bikini,
          ponytail, blonde_hair, green_eyes, masterpiece, best_quality
          └─用户说的─┘  └──── 模型自己猜的，而且猜错了 ────┘

「比安卡」当时不在词库里（战双帕弥什整个游戏都缺），所以：
- 模型补了 `ponytail` / `blonde_hair` / `green_eyes` —— 用户一个字都没提
- 模型还把「泳装」拆成 `swimsuit` + `bikini`（两个不同款式，同时生效会打架）
- 用的角色 tag 也是错的：官方是 `bianca_(pgr)`，模型编了个不存在的

修法分三层：

- **提示词加 3 条硬约束**（规则 11/12/13）
  - 11：禁止添加用户未提及的外观；不认识的角色只输出角色名，不许猜长相
  - 12：禁止输出近义标签（`swimsuit`+`bikini` → 只留一个）
  - 13：不确定就不加，少而准远好过多而猜
  - 同时把规则 3 的 `only normalize/complete them` 改成 `keep them VERBATIM`
    —— 原来的 `complete` 等于鼓励模型补全，和规则 6「别乱动」自相矛盾
- **代码兜底 `_collapse_synonyms()`**：结果返回前折叠近义标签
  （`swimsuit/bikini`、`1girl/female`、`1boy/male`），模型没听话也不会出问题
- **补词库**：新增战双帕弥什 19 条、鸣潮 32 条

### 修复：substring 模式英文标签粘连

连写输入替换后标签会粘成一坨，而且**用户看不出来**：

    输入「鸣潮达尼亚」→ 修复前: wuthering_wavesdenia_(wuthering_waves)
                      → 修复后: wuthering_waves denia_(wuthering_waves)

这串东西会原样发进生图请求，模型和 NovelAI 都读不懂。原因是
`replace_substring()` 用裸 `str.replace()` 没补分隔符，现在替换时前后补空格
并收敛多余空白。

### 词库扩充

| 分组 | 条数 | 说明 |
|------|------|------|
| 战双帕弥什 | 19 | 此前**完全缺失**，是本次事故的直接原因 |
| 鸣潮 | 33 | 此前只有「鸣潮」1 条，补齐主要角色 |

总数 1390 → **1440 条**（角色 746 → 797）。

> 战双的 Danbooru 官方 tag 用 `(pgr)` 后缀（`bianca_(pgr)`），
> 不是 `(punishing:_gray_raven)`；鸣潮的达妮娅官方罗马音是 `denia_`，
> 和丹瑾 `danjin_` 是**两个不同角色**，注意别混。

### 其他

- 新增 16 项回归测试（110 项），覆盖：substring 粘连、近义折叠、新增角色词条
- README 新增「防止模型脑补」章节，含真实案例对比

## v1.2.0

这一版全在解决「翻译模型翻不准、翻得慢」的问题。

- **新增词库直译（不走模型）** —— 命中词库的词直接查表替换，压根不调模型
  - 内置 **1390 条**词库，分三个文件：角色名（746 条）、
    外观（400 条）、场景（250 条）
  - 角色名覆盖 19 个作品分组：原神 / 崩坏星穹铁道 / 绝区零 / 蔚蓝档案 /
    明日方舟 / Fate 系列 / 东方 Project / 经典动画 / hololive·彩虹社等 VTuber /
    单机游戏（最终幻想·尼尔·生化危机·守望先锋等）
  - 跨作品重名角色（如「爱丽丝」同时是蔚蓝档案 / 东方 / 绝区零的角色）一律写
    带作品限定的词条，不做无根据猜测 —— 猜错比不收录更糟
  - 角色名全部对照 Danbooru 官方标签，**不做音译** —— 「雷电将军」永远翻成
    `raiden shogun`，不会这次是 `raiden shogun` 下次变 `thunder general`
  - 流程改成「先查表，没命中的部分才交给模型」：全命中时**一次模型都不调**，
    部分命中时只把未命中片段发给模型（模型工作量最小，也最不容易翻车）
  - 实测：纯属性/角色类输入 **100% 免模型**，整体词条级命中率 82%
  - 两种匹配模式：`segment` 逐词精确（默认，不会误伤）/ `substring` 内嵌替换
    （能命中「白发少女站在樱花树下」这类连写句）
  - NovelAI 权重语法（`1.3::tag::`、`{{tag}}`、`[tag]`、`artist:name`）
    在两种模式下都会被**整体保护**，不会被切碎或替换
- **新增自定义词库** —— 用户可加自己的词条，不用改源码
  - 配置项「自定义词库文件路径」填一个 `.json` 或 `.txt` 路径即可
  - JSON 带分组便于维护，TXT 支持 `中文=英文` / `中文,英文` 和 `#` 注释
  - **用户词条优先级高于内置**，觉得内置哪条翻得不对就直接覆盖
  - 内置词库文件被插件更新覆盖，所以刻意设计成「用户永远不动内置文件」
- **新增翻译结果校验** —— 拦掉「模型翻一半就交卷」
  - 检查结果里有没有残留中文，有就自动重翻（重翻时明确告诉模型「你上次没翻干净」，
    比原样再问一次有效得多）
  - 重翻后仍有中文时按配置处理：`warn` 提醒但继续生图 / `abort` 终止不生图（省点数）
  - 这一步很必要：模型遇到不认识的中文会直接抄回来，界面上看起来「翻过了」，
    但 NovelAI 完全读不懂，用户只会觉得「怎么画出来的不是我说的东西」
- **给系统提示词加 few-shot 示例** —— 原来只有 9 条规则没有例子，模型容易「飘」
  - 加了 6 组对照示例，每组对应一类实际翻车：长句直译成英文句子、角色名音译、
    权重语法被「顺手规范化」、已英文输入被擅自重写、抽象氛围不会提标签、量词处理
  - 新增第 10 条规则：中文量词要转成标签（双马尾 → twintails，两把刀 → holding two swords）
- 新增 `/nai dict`（`/nai 词库`）指令：
  - 不带参数看词库概况（内置/自定义条数、匹配模式）
  - 带参数可就地测试命中情况，`/nai dict 白发 双马尾` 会告诉你哪些命中、
    哪些还得走模型 —— 加完自定义词条不用真生一张图去验证
- 词库直译与模型翻译结果拼接时**按小写去重** —— 两边都产出 `white hair` 时
  只保留一个（重复标签在 NovelAI 里等于加权，会让画面过拟合）

## v1.1.0

- **新增图生图（img2img）**，后端是**通用 HTTP 模板**，不绑定任何厂商
  - 为什么不做死在 Nai2API 上：网关的 `/generate` 接口没暴露 NovelAI 的
    `reference_image_multiple`（`action` 被硬编码成 `generate`，参考图字段恒为空数组），
    但用户手上可能有别的支持图生图的渠道。所以做成「用户对着渠道文档填请求模板」
  - 支持 `multipart/form-data`（上传文件）和 `application/json`（base64 内嵌）两种提交格式
  - 模板占位符：`{{prompt}}` `{{negative}}` `{{image_base64}}` `{{image_data_url}}`
    `{{strength}}` `{{noise}}` `{{seed}}` `{{model}}` `{{size}}`
  - **未知占位符原样保留**并打日志告警，不静默清空 —— 模板写错名字时一眼能看出来
  - 响应解析按「Content-Type 是图片 → 指定路径 → 扫常见字段 → 解 base64/data URL」
    四级降级，`response_path` 支持 `data.0.b64_json` 这种点分路径
  - 配置块默认**隐藏**（`img2img_enabled` 开关控制），不想用的人配置面板不会被塞满
- **触发方式**：回复一张图再发 `/nai`，自动识别为图生图
  - `--i2i` 强制图生图（找不到参考图会明确报错，不静默降级）
  - `--no-i2i` 强制文生图
  - 自动模式下取图失败会提示「本次改为文生图」，不装作没事发生
- **图生图参数**：`--strength` / `--noise` 指令内临时覆盖，配置里给默认值
  - 解析器把 `--strength` / `--noise` / `--i2i` / `--no-i2i` 加进了正则的
    停止标记集合，否则它们会被 `--artist` / `--negative` 的贪婪匹配吞掉
- **图生图不走二次确认**：它走的是用户自己的渠道，扣点规则和 Nai2API 无关，插件无法预估
- 信息标签新增图生图分支：显示「图生图 | 相似度0.60」而不是没意义的模型名

- **修复：`/nai save` 可以覆盖内置预设**
  用户以为在备份自己的组合，实际把内置的 `GalGame风` 改掉之后，
  自己再 `/nai -p GalGame风` 拿到的就不是文档里那个效果了，很难排查。
  现在直接拒绝并给出改名建议。
- **修复：合并转发在多平台崩溃**
  Telegram 之类的 sender id 不是纯数字、部分平台的 `message_obj` 没有 `sender` 属性，
  原来的代码会 `AttributeError` 导致整条消息发不出去。现在逐级降级取昵称。
- **修复：V4.5 模型在信息标签里显示成 `v4-5`**
  模型名里的版本号本身带连字符（`nai-diffusion-4-5-full`），
  简单的点号替换处理不了，现在单独把 `4-5` 归一成 `4.5`。

## v1.0.1

- **OpenAI 兼容接口配置重做**（不再是一个多行文本框）
  - 拆成「接口地址」「接口密钥」两个独立输入框，密钥用 `secret` 掩码显示，不再明文暴露
  - 新增「OpenAI 接口」预设下拉：OpenAI 官方 / 阿里云百炼 / DeepSeek / 硅基流动 /
    Kimi / 智谱 / OpenRouter / 本地 Ollama，选中后**自动填好地址和常用模型**
  - 「直译模型」可**实时拉取**接口的 `/models` 生成下拉列表，点一下就能选；
    拉不到（网络不通 / 没 key / 接口不支持）会静默退化成手填输入框，不影响配置面板渲染
  - 地址容错：只填域名会自动补 `/v1`，误把 `/chat/completions`、`/models` 填进去会自动去掉
  - 拉取逻辑兼容已有事件循环（线程池 + `asyncio.run`），不会把异步框架卡死
- `image_manager.save_image()` 新增 `prefix` 参数，为后续图生图的源图存档预留能力

## v1.0.0（首个发布版）

- **NovelAI V5 模型支持**，并按「模型 + 尺寸」双维度区分扣点
  - V4.5 普通图 1 点，V5 普通图 5 点，2K 15 点，4K 25 点
  - V5 普通尺寸、2K、4K 都会先二次确认，防误扣
- **提示词直译**：中文/英文都自动翻成 NovelAI 英文标签
  - 保留 `1.2::tag::`、`{{tag}}`、`-2::tag::` 等权重语法和 `artist:name` 画师标签
  - 直译模型支持下拉框勾选（可多选轮询，失败自动切换）
  - 也支持自定义 OpenAI 兼容接口
- **`-m` 参数切换模型**：`/nai -m 5 -p 动漫风 1girl, silver hair`
- **自定义命令名**：`command_names` 支持 `nai,niu,绘` 等多个别名
- 图片本地缓存、自定义预设、余额查询、LLM 工具调用

详细演进过程见下方历史版本记录。

## 历史版本（内部迭代，未单独发布）

### v1.3.1（旧编号，与本仓库 v1.3.1 无关）

- **翻译模型改成下拉框勾选，不用再手抄 provider id**
  - 原来 `translate_provider_ids` 是个文本框，要手动填 AstrBot 的模型 ID，
    但那个 ID 是一长串 uuid，很难抄、很容易填错
  - 现在改用 AstrBot 官方的 `_special: select_providers` 机制，
    配置面板会渲染成一个按钮，点开直接列出 AstrBot 里已配置的模型，勾选即可
  - 支持多选：勾多个就按顺序轮询，前一个失败自动换下一个
  - 一个都不勾仍然是「自动发现全部可用对话模型」，行为和之前一致
  - 配置类型从 `text` 改成 `list`，代码侧 `_split_list` 同时兼容「列表」和
    「逗号分隔字符串」两种形式，老配置不会失效
- 顺带把插件名改成 `astrbot_plugin_nai_plus`（原 `astrbot_plugin_nai2api`），
  这样能和原版插件同时安装，不会互相顶掉；README 补充了对原作者 helloWKQ 的致谢

### v1.3.0

- **新增提示词直译：中文/英文都自动翻成 NovelAI 能认的英文标签**
  - 中文直接写就行，不用自己翻成英文标签：`/nai 一个银发蓝眼的女孩`
  - 英文描述也会被规范化：`/nai a girl with long hair` → `1girl, long hair`
  - 直译发生在生成之前，所以二次确认里显示的是最终真正送出去的提示词
  - 内置提示词会强制模型保留 NovelAI 的权重语法（`1.2::tag::`、`{{tag}}`、`[tag]`、`-2::tag::`、
    `\n20::tag::`）和 `artist:name` 这类画师标签，不会被翻译破坏
  - 模型爱加的 markdown 代码块、引号、`Tags:` 前缀会被自动清洗掉
- **直译模型两套接入方式，配置里切换**（`translate_mode`）
  - `astrbot`（默认）：直接用 AstrBot 里已经配好的模型，**装完就能用，不用另外填 key**
    - 不填 provider id 时会自动发现 AstrBot 所有可用的对话模型
    - 会自动跳过 TTS/STT/Embedding 这类不能翻译的模型
  - `openai`：调用你自己填的 OpenAI 兼容接口
    - 格式：`base_url|api_key|model`，一行一个
    - 适合用别的便宜模型专门干翻译这件事
- **翻译模型支持轮询/失败切换**：配多个模型，前一个失败（超时、限流、欠费、返回空）自动换下一个，
  全部失败才报错。成功的那个会被记住，下次优先用它
- **翻译失败可选策略**（`translate_on_error`）
  - `fallback`（默认）：翻译失败就用原文继续生图，并提示一句
  - `abort`：直接终止，避免翻译失败还白白扣点
- **新增 `-m` 参数切换模型**：`/nai -m 5 -p 动漫风 1girl, silver hair`
  - 支持简写：`5`、`5c`、`4.5`、`4`、`3`、`furry`、`2`、`safe`，也可以写全名
  - 也能用长参数 `--model`
  - 写错模型名会原样保留（不会静默换成默认模型，免得你以为切成功了）
  - LLM 工具 `nai_generate` 也加了 `model` 参数
- **新增自定义命令名/别名**（`command_names`）
  - 默认 `nai`，可以改成 `nai,niu,绘` 这样，多个名字指向同一个功能
  - 在 `__init__` 里用 AstrBot 的 `Context.register_commands()` 动态注册
    （装饰器是导入时就执行的，那时读不到用户配置，所以必须动态注册）
  - 老版本 AstrBot 没有这个 API 时会给出提示并回退到默认的 `nai`，不会崩
  - ⚠️ 「`/`」或「`+`」这种**前缀符号是 AstrBot 的全局设置**（`data/cmd_config.json` 里的
    `wake_prefix`），插件改不了。想做 `+nai`，需要把全局前缀设成 `+`，这里仍填 `nai`
  - 改完命令名需要**重载插件**才生效
- 帮助文本改为按实际命令名渲染，并补上 `-m` 说明和中文示例

### v1.2.0

- 新增 NovelAI V5 系列模型支持
  - 模型列表加入 `nai-diffusion-5-full`、`nai-diffusion-5-curated`
  - 默认模型改为 `nai-diffusion-5-full`
  - V5 变体（如未来的 5-curated-inpainting）按 `nai-diffusion-5` 前缀自动识别
- **重要：按模型区分扣点，并新增 V5 普通尺寸的二次确认**
  - Nai2API 的扣点是「模型 + 尺寸」两个维度决定的，不是只看尺寸：
    - V4.5 普通图 **1 点**，2K 15 点，4K 25 点
    - V5 普通图 **5 点**，2K 15 点，4K 25 点（V5 普通图就是 5 倍）
  - 原来的确认逻辑只拦 2K/4K，拦不住「V5 + 普通尺寸 = 5 点」这个坑，现在一并拦下
  - 确认提示会显示具体原因和点数，例如：
    `本次使用模型「nai-diffusion-5-full」的普通尺寸，将消耗 5 点`
  - LLM 工具调用时同样拦截，并提示模型可以让用户改用 V4.5
- 余额查询改为按当前配置的模型计算
  - 原来一律按「普通图 1 点」估算，选 V5 时会算错 5 倍
  - 现在会显示当前模型、每张单价和预估张数
- 新增扣点计算模块 `get_generation_cost()` / `is_v5_model()`，集中管理扣点规则
- 帮助文本补充扣点说明表

### v1.1.0

- 新增「修改预设」功能
  - 指令：`/nai update <名称> <新的质量前缀>` 或 `/nai 修改 <名称> <新的质量前缀>`
  - LLM 工具：`nai_update_preset`（参数：name, artist）
  - 内置预设不可修改，自定义预设才能修改
  - `PresetManager` 新增 `update()` 方法，支持只修改部分字段
- 帮助信息和 docstring 同步更新

### v1.0.11

- 真正修复 Gemini 等模型通过 OpenAI 兼容层调用 LLM 工具时报 `value at top-level must be a list` 错误的问题
- 根本原因：AstrBot 框架 `register_llm_tool` / `spec_to_func` 生成的 JSON Schema 不包含 `required` 字段，Gemini API 对工具 Schema 校验严格，缺少 `required` 字段时返回 400 错误
- 修复方案：在插件初始化后，手动给 5 个 LLM 工具的 `parameters` 添加 `required` 字段，绕过框架限制
- 涉及工具：`nai_generate`(prompt)、`nai_get_balance`(detail)、`nai_list_presets`(preset_name)、`nai_save_preset`(name, artist)、`nai_delete_preset`(name)
- v1.0.10 的"必填参数"方案无效，因为框架根本不根据参数默认值生成 `required` 字段

### v1.0.10

- 彻底修复 Gemini 等模型通过 OpenAI 兼容层调用 LLM 工具时报 `value at top-level must be a list` 错误的问题
- 根本原因：AstrBot 框架 `spec_to_func` 生成 JSON Schema 时不设置 `required` 字段，当工具所有参数都有默认值（零必填）时，Gemini 校验失败
- 将 `nai_get_balance` 的 `detail` 参数改为必填（去掉默认值 `"simple"`）
- 将 `nai_list_presets` 的 `preset_name` 参数改为必填（去掉默认值 `""`），内部逻辑重写：填 "all"/"全部" 列出所有预设，填具体名称查看单个预设
- 至此所有 5 个 LLM 工具均至少有一个必填参数，Schema 生成正常

### v1.0.9

- 修复 LLM 工具调用与部分 AI 模型（如 Gemini）的兼容性问题
- 将 `nai_generate` 工具的 `seed` 参数从 int 类型改为 string 类型，避免 Schema 验证错误
- 为 `nai_get_balance` 工具添加 `detail` 参数，解决零参数导致的 Schema 生成问题
- 函数内部自动处理类型转换，不影响正常使用

### v1.0.8

- 新增 `AI_MODIFY_RULES.md` AI 修改要点文档（记录修改规范、退回方法、沟通注意事项）
- 补更新 CHANGELOG.md 和 README.md（之前漏掉了）
- 开发者说明：零基础新手

### v1.0.7

- 生图成功后显示信息标签：`预设名 | 耗时X秒`
- 生图失败时也显示信息标签，包含简要失败原因
- 插件配置新增 `show_image_info` 开关（默认开启）
- 指令生图和 LLM 工具生图都支持

### v1.0.6

- 修复合并转发消息功能丢失的问题（合并分支时被覆盖）
- 恢复查询类结果使用合并转发消息发送（余额查询、预设列表）
- 图片文件名包含提示词，方便服务器上查找管理

### v1.0.5

- 生成的图片文件名包含提示词（清洗后），方便在服务器上查找和管理

### v1.0.4

- 新增 CHANGELOG.md 更新日志文件

### v1.0.3

- 查询类结果（余额查询、预设列表）改用合并转发消息发送，不再刷屏

### v1.0.2

- 添加 LLM 工具调用：查询余额（nai_get_balance）
- 添加 LLM 工具调用：预设管理（nai_list_presets、nai_save_preset、nai_delete_preset）
- 修复 LLM 工具调用结果不发送给用户的问题

### v1.0.1

- 修复 `/nai save` 指令参数丢失的问题
- 添加查看单个预设详情功能（`/nai presets <预设名>`）

### v1.0.0（原版项目的初始版本，仅作追溯保留）

- 初始版本
- `/nai` 指令文生图，支持尺寸、预设、质量前缀、负面提示词、随机种子
- LLM Tool 调用生图（nai_generate）
- 5 个内置预设 + 自定义预设保存/删除
- 支持普通/2K/4K 分辨率
- 图片本地缓存，自动清理
- 配套人格提示词（生图助手）
