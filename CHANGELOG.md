# 更新日志

## v0.3.2 (2026-09-19)

> 对直译提示词的逐条校对 + 一处**静默丢内容**的修复。**不改动任何直译架构**（决策顺序、只读 provider、
> 失败回退、路由、前端交互全部保持）。

- **修复角色名不能一律套括号（会造出不存在的标签）**：
  - **为什么**：v0.3.1 的规则 7 写死了「格式为 `角色名_(作品名)`」，但**大量 Danbooru 角色标签本身不带括号**
    （最典型 `hatsune_miku`）。按旧措辞，初音未来会被翻成 `hatsune_miku_(vocaloid)` —— 这是个**不存在的标签**，
    NovelAI 会当噪声忽略甚至误算权重。
  - 现改为**条件式**：带括号消歧的才写 `角色名_(作品名)`（如 `raiden_shogun_(genshin_impact)`）；
    **本身不带括号的就直接用它，不要画蛇添足加括号**（如 `hatsune_miku`，不是 `hatsune_miku_(vocaloid)`），
    并补了「初音未来 -> hatsune_miku」对照示例。
- **修复多行输出被静默截断（本次踩坑）**：
  - **为什么**：规则 4 要求原样保留 `\n20::tag::`，但模型偶尔把标签**分多行**输出；老 `_clean_llm_output`
    只保留第一行，**第二行被静默丢弃**，而且丢弃后仍能通过中文校验 —— 属于静默丢内容，违反项目铁律。
  - **代码**：`_clean_llm_output` 多行分支改为「保留**所有**像标签的行并用 `, ` 拼接」；
    像标签判定 = 行非空、长度 ≤ 200、不以句子结束标点（。！？.!?）结尾。
    ⚠️ 结束标点集合**不含冒号**，以 `::` 结尾的权重分组（`1.3::silver hair::`、`\n20::best quality::`）必须保留（有回归测试守着）。
  - **提示词**：规则 4 补充「`\n20::` 里的 `\n` 是字面的反斜杠 + 字母 n，不要输出真正的换行符」。
- **超时文案修正**：`timeout < 1` 时 `int(timeout)` 会显示成「超时 0s」，改用 `{timeout:g}`（如 `0.5` 显示为 `0.5s`）。
- **其余提示词打磨（低成本）**：
  - 规则 15：把「忽略尺寸、分辨率等与画面无关的指令」改为「尺寸、分辨率这类词**不要翻译**；其余内容照常翻译、不要遗漏」
    （「忽略」有被弱模型泛化成「丢掉相关内容」的风险）。
  - 规则 14：示例 `两把刀 -> holding two swords` 与规则 11「绝不编造姿势」矛盾，改为 `两把刀 -> two swords`。
  - 规则 12：补「仅在能确定画面有且仅有一个人物时才加 `1girl`/`1boy`；已有 `1girl`/`1boy`/人数标签则保持其原位置不挪动」；
    并补多人场景「明确描述多个人时按人数输出 `2girls` / `3girls` / `multiple girls`」。
  - 规则 13：改为「只有确实同义 / 重复时才合并」，并说明不同强度标签**不要**合并（如 `long hair` 与 `very long hair` 都要保留）。
- `_conf_schema.json` 的 `default` 已同步为**逐字节一致**（现 2127 字符），一致性断言继续通过。
- `metadata.yaml` → `0.3.2`；`_PLUGIN_VERSION_FALLBACK` 同步；README 配置表 `translate_system_prompt` 行同步说明。

## v0.3.1 (2026-09-19)

> 对 v0.3.0 直译功能的三处校对修正。**不改动任何直译逻辑**（决策顺序、只读 provider、失败回退与 note 文案、
> 路由、前端交互全部保持），只改默认提示词本身与一处前端版本号兜底。

- **默认系统提示词改为中文（`DEFAULT_TRANSLATE_SYSTEM_PROMPT`）**：
  - **为什么**：该字段在 WebUI 里是**给用户看、给用户改**的输入框，而本插件面向**零基础中文用户**。
    原先 3001 字符的纯英文会让「可编辑」形同虚设。现改写为**中文规则条文 + 中文输入→英文标签输出对照示例**，
    共 1705 字符，用户能读懂、能自行修改。
  - 英文标签、权重语法（`1.2::tag::` / `{{tag}}` / `[tag]` / `-2::tag::` / `\n20::tag::`）、`artist:name`
    一律**原样保留**不翻译。
  - `_conf_schema.json` 的 `default` 已同步为**逐字节一致**，`TestPromptTranslation::test_default_prompt_matches_schema` 继续通过。
- **修正「不知道角色消歧写法时」的歧义（最重要）**：原文 `…output the romanized character name as a tag and STOP.`
  会被模型理解成「后面不用翻了」，导致用户其余的服装/动作/场景描述**整段丢失**。现明确为：
  该角色只输出「小写 + 下划线」的罗马字名字（**不加括号、不编造作品名、不猜测长相**），
  **其余描述照常翻译，绝不因一个角色不认识就丢内容**；并新增「不确定角色属于哪个作品时，宁可不加括号，也绝不编造作品名」。
- **修正「示例」与「禁止脑补」规则的矛盾**：原「赛博朋克…氛围感」示例输出 `cinematic lighting, depth of field`，
  属于凭空补标签，与「绝不编造用户未提到的细节」相冲突。已采用方案 A：把该示例改为不含脑补的写法
  （`赛博朋克风格的城市夜景，霓虹灯，雨` → `cyberpunk, city, night, neon lights, rain`）。
- **前端兜底版本号修正**：`pages/nai-config/app.js` 中 `data.version || "0.0.1"` 的硬编码默认已移除，
  取不到版本时只显示「已连接」，不再把 `0.0.1` 误当成当前版本。

## v0.3.0 (2026-09-19)

> **新增「中文提示词直译」。** 以前想用中文描述画面，只能自己硬翻成英文标签，翻错就废图。
> 这一版让插件在你输入中文时，自动调用一个**独立的对话模型**把中文直译成英文 Danbooru 标签再送出生图。
>
> **为什么这么设计：**
> - **直译模型与聊天主模型彻底分离**：直译只做「读」操作（列出模型 / 取模型 / 调 `text_chat`），
>   **绝不**修改你的对话主模型设置（不调用 `set_using_provider`），互不干扰。
> - **只在含中文时才触发**：纯英文 / 符号提示词原样透传，**不会**调用任何 LLM，既省 token 又不改变既有行为。
> - **失败绝不静默（铁律）**：直译失败时**回退用原文生图**，并在图片信息下方明确写出失败原因
>   （未配置模型 / 超时 / 模型返回异常 / 结果仍含中文等），而不是让生图直接失败。
> - **直译结果直接进入生图链路**：不做二次确认、不落盘、不写配置，测试按钮也只用「未保存的表单值」。

- **新增 `core/translate_client.py`（唯一真源）**：核心为 `PromptTranslator`，负责解析直译模型、
  组织系统提示词、调用 `provider.text_chat` 并清洗/校验返回值。
  - `has_cjk()` 判定是否含中文；`_is_chat_provider()` 过滤出可对话模型（排除 tts/stt/embedding 等）。
  - `_clean_llm_output()` 只做「去噪」（剥代码块 / 引号 / 解释性前缀 / 收敛空白与重复逗号），
    **不做语义改写**，并刻意不拆分类单个逗号，避免破坏 `1.5::a, b::` 这类内部含逗号的权重分组。
  - `translate()` 严格决策顺序：空 → 未启用 → 不含中文 → provider 解析失败 → 调用 → 清洗校验；
    任一步失败都返回 `TranslateResult(text=原文, translated=False, note=原因)`。
  - 调用带超时（默认 30s，可用 `translate_timeout` 覆盖）。
- **接入生图主链路**：`/nai` 指令（`_handle_generate`）与 LLM 工具（`nai_generate_tool`）
  在组装提示词前先直译用户提示词；图片信息行新增「 | 直译」标记，失败时另起一行说明原因。
- **新增 WebUI「提示词直译」卡片**：勾选启用、下拉选择直译对话模型、可视化编辑系统提示词、
  一键「恢复默认」、以及「测试直译」按钮（使用未保存的表单值试译，不写盘、不改内存）。
- **新增 3 个后端接口**：`translate/models`（GET，列出可用对话模型）、`translate`（POST，保存配置）、
  `translate/test`（POST，试译不保存）；`config`（GET）返回值新增 `translate` 与 `translate_default_prompt`。
- **新增 3 个配置项**：`translate_enabled`、`translate_provider_id`、`translate_system_prompt`
  （默认提示词与 `DEFAULT_TRANSLATE_SYSTEM_PROMPT` 逐字节一致，测试中已加一致性断言）。

## v0.2.2 (2026-09-18)

> **这是一次「找到真凶」的修复版本。** 用户此前反馈的「`-p` / `-m` 命令没用、风格不生效」，
> 根源是 AstrBot 框架对指令参数的处理方式，而非用户操作问题。

- **修复致命的指令参数被截断 bug（真凶：GreedyStr 默认值踩坑）**：
  - **现象（用户真实后台日志为证）**：用户输入 `#nai -m 5 1girl`，日志却显示
    `提示词: -m, 1girl, ...` 且模型仍是默认的 V4.5 → `5` 和 `1girl` 丢失，只收到第一个词 `-m`。
  - **根因**：`main.py` 中指令签名写成了 `async def nai_cmd(self, event, args: GreedyStr = "")`。
    AstrBot 的 `astrbot/core/star/filter/command.py::CommandFilter.init_handler_md()` 里，
    参数**带默认值**时写入 `handler_params` 的是**默认值本身**（此处为 `''` 普通字符串），
    而不是注解类型 `GreedyStr`：
    ```python
    if v.default == inspect.Parameter.empty:
        self.handler_params[k] = v.annotation   # 无默认值 -> 存 GreedyStr 类型
    else:
        self.handler_params[k] = v.default      # 有默认值 -> 存 '' 这个普通字符串
    ```
    随后 `validate_and_convert_params()` 用 `is_greedy = param_type_or_default_val is GreedyStr`
    判断：拿到的是 `''`（普通 str）→ `is_greedy=False` → 走 `isinstance(..., str)` 分支**只取第一个 token**。
  - **修复（P0-1）**：去掉 `= ""` 默认值，恢复 `args: GreedyStr`。空参场景仍安全
    （AstrBot 在 `message_str == 'nai'` 时传空串）。代码中已写明注释与踩坑记录，防止后人再改回去。
  - **第二道保险（P0-2）**：新增 `_recover_command_text(event)`，直接从**原始消息文本**还原
    「指令名之后」的完整参数。`nai_cmd` 中采取「谁更长用谁」策略，正常情况两者一致无副作用，
    异常时打 `warning` 日志（禁止静默）。即使框架行为再变，也不会再丢参数。
- **新增版本自检指令 `/nai version`（别名 `版本`）（P0-3）**：
  - 返回插件版本、当前默认模型、当前默认预设，并提示「指令前缀以你配置的唤醒前缀为准」。
  - 插件启动时读取 `metadata.yaml` 的版本号存入 `self._plugin_version`，并打印启动日志
    `已加载，版本 vX.X.X`，方便用户一眼定位版本（不用再猜）。
- **新增预设名别名解析（P1-4）**：
  - 用户习惯输入「韩漫风」，而内置全名是「韩漫小清新风」；现在会自动映射。
  - `core/preset_manager.py` 新增 `PRESET_ALIASES` 别名表、`_norm_preset_key()` 与
    `resolve_preset_name()`，以及 `PresetManager.resolve()` 便捷方法。
  - 匹配顺序：精确匹配 → 别名表 → 忽略大小写/空格 → 唯一子串匹配；**只做「输入→真实全名」映射，
    绝不新建或覆盖预设，解析不到返回 None 由上层报错（禁止静默降级）**。
  - 已接入 `-p` 生图、`/nai default`、`/nai presets <名>`、`/nai update`、`/nai del` 及 LLM 工具；
    解析成功且与原名不同时打 `info` 日志留痕。
  - **未**在 `/nai save`（新建预设不该被改名）与 WebUI `api_presets`（精确 id 操作）中启用。
  - 预设不存在的报错现在**直接列出所有可用预设名**，用户无需再发一条 `/nai presets`。
- **帮助文案适配真实唤醒前缀（P1-5）**：
  - 用户把唤醒前缀从 `/` 改成 `#` 后，帮助与引导文案不再写死 `/nai`，而是按实际前缀显示
    （通过 `_cmd_head(event)` + 整体 `.replace("/nai", ...)` 产出，避免逐个插值漏改）。
- **完善自动化测试**：
  - 新增回归守卫：断言 `nai_cmd` 的 `args` 参数**没有默认值**（防止后人手贱写回 `= ""`）。
  - 新增端到端复现测试：模拟 `args` 被截断为 `-m`，原始消息为 `#nai -m 5 1girl`，
    断言最终 `client.generate` 收到 `model == "nai-diffusion-5-full"` 且 prompt 不含 `-m`。
  - 新增预设别名解析、别名报错列出可用预设、`/nai version` 含版本号等测试。

## v0.2.1 (2026-09-18)

- **增强 `-p` 与 `-m` 参数的容错清洗与标点兼容**：
  - 新增 `_clean_param_val()` 参数清洗机制，自动去除用户可能误带的外层尖括号（`< >`）、书名号（`《 》`）、中文方括号（`【 】`）、引号及末尾逗号/分号（如 `-p <动漫风>` 自动识别为 `动漫风`，`-m <5>` 自动识别为 `5` 并解析为 V5 模型）。
  - 扩展模型正则匹配规则为 `\S+`，防止尖括号等特殊字符被截断。
- **新增独立的全局模型查询与切换命令**：
  - 新增 `/nai model`（或 `/nai 模型`）：查询当前默认模型，并列出常用切换命令。
  - 新增 `/nai model <模型序号/名称>`（如 `/nai model 5`、`/nai model 5c`、`/nai model 4.5`）：一键切换全局默认生图模型并自动保存。
- **支持单独发送 `-m` / `-p` 快捷设置**：
  - 当用户单独发送 `/nai -m 5` 或 `/nai -p 动漫风`（未附带提示词）时，自动识别并将其作为快捷设置处理（分别切换默认模型或默认预设），不再报“提示词不能为空”。
- **智能化未输入提示词引导**：
  - 当用户在生图调用中仅传入了 `-p` 预设或 `-m` 模型却漏掉描述词时，返回友好的双重操作指引（引导用户单次加词生图，或指导其设为全局默认）。
- **完善自动化测试**：
  - 增加对符号清洗、尖括号参数提取、独立切换子命令及智能引导的单元测试覆盖。

## v0.2.0 (2026-09-18)

- **支持 NovelAI V5 双模型**：
  - 新增 `nai-diffusion-5-full` (NovelAI Diffusion V5 Full) 和 `nai-diffusion-5-curated` (NovelAI Diffusion V5 Curated) 支持。
  - 在配置项 `default_model` 中将 V5 双模型排在最前，并在配置提示中说明其二次元细节与自然语言支持特性。
- **指令 `-m` / `--model` 快速切换模型**：
  - `/nai` 指令增加 `-m` / `--model` 参数支持，支持使用别名快速指定生图模型（如 `-m 5`、`-m 5c`、`-m 4.5`、`-m furry` 等）。
  - 提供 `MODEL_ALIASES` 字典与 `resolve_model_alias()` 解析函数，智能映射模型简写与全称。
  - 提供 `is_v5_model()` 判断模型是否属于 V5 架构。
- **LLM 生图工具扩展**：
  - `nai_generate_tool` 增加 `model` 参数，支持大模型在调用生图工具时指定所用模型。
- **生图信息与扣点提示**：
  - 生图信息标签中增加当前所用模型标识（如使用 V5 或显式指定模型时提示）。
  - `/nai balance` 余额查询中增加 V4.5 (1点/张) 与 V5 (5点/张) 普通尺寸预计可生成张数区别说明。

## v0.1.2 (2026-09-18)

- **修复生图完成后的图片保存异常**：
  - 修复 `main.py::_do_generate` 中调用 `ImageManager.save_image()` 时误传 `prompt=prompt` 导致 `unexpected keyword argument 'prompt'` 的缺陷。
  - 修复 `save_image()` 未使用 `await` 异步等待的问题。
  - `ImageManager.save_image()` 签名增加 `**kwargs` 兼容，防止任何未声明的关键字传参报错。

## v0.1.1 (2026-09-18)

- **修复 WebUI 面板 Bridge 通信与路由问题**：
  - 前端引入异步 Bridge 轮询解析器（`resolveBridge`）并改用 `type="module"`，确保在 AstrBot 仪表盘 iframe 异步注入 `bridge-sdk.js` 后能稳定建立通信，消除「无法与 AstrBot 建立通信」的提示。
  - 前端增加 `bridge.ready()` 与 `bridge.onContext()` 主题和生命周期同步。
  - 后端 Web API 同步注册带有 `/{PLUGIN_NAME}/` 前缀的官方标准路由与相对路由，保证不同 AstrBot 路由转发器都能精确匹配。

## v0.1.0 (2026-09-18)

- **独立 WebUI 预设管理面板**：
  - 在 AstrBot 插件管理页面中增加独立的「NovelAI 预设管理」页面（`pages/nai-config/`），通过 AstrBot 原生 Pages 机制与 Web API 桥接，无需手动改写配置文件。
- **三段式预设模型**：
  - 预设全面升级为 **质量词/画师串**、**附带正向词**、**附带负向词** 三段式结构：
    - **质量词/画师串**：作为独立 `artist` 参数发送给 Nai2API 网关，保持 NAI 原生语法隔离，画师串中负权重标签（如 `-1.5::xxx::`）自动提取至负面词。
    - **附带正向词**：自动附加在用户提示词末尾，补充风格与光影效果。
    - **附带负向词**：自动与全局默认负向词智能去重合并。
  - 智能构图兜底：当提示词和预设均未指定景别时，自动补充 `full body, standing`。
- **默认预设功能**：
  - 支持在 WebUI 中一键将任意内置或自定义预设设为默认预设，或取消默认预设。
  - 指令端同步增加 `/nai default [预设名]`（或 `/nai 默认 [预设名]`）查询、设置和取消。
  - 支持生图参数 `--no-preset`，在已设置默认预设的情况下可单次临时禁用预设。
- **数据与配置双向互通**：
  - WebUI 面板操作即时回写 AstrBot 插件配置 `custom_presets`（`template_list`）和本地数据文件，与 `/nai save`、`/nai update`、`/nai del` 指令互通共享。
- **实时模拟预览**：
  - WebUI 页面提供测试提示词与实时预览框，所见即所得查看最终组装的 artist / tag / negative 参数。

## v0.0.1 (2026-09-18)

- **全新初始版本**：从原版纯净架构重新起步，版本从 0.0.1 开始迭代。
- 支持 `/nai` 文生图指令（尺寸、预设、画师前缀、负面提示词、随机种子）。
- 支持 5 个官方内置预设（2.5D唯美风、韩漫小清新风、本子动漫风、GalGame风、动漫风）及自定义预设增删改查。
- 支持 LLM 工具调用生图（支持 AI 助手直接调用）。
- 支持生图信息标签、余额查询与本地图片缓存自动清理。
