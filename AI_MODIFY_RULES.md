# AI 修改要点文档

> **重要**：每次帮用户修改代码前，必须先读本文档！
>
> **开发者**：零基础新手（咲磊 / Shawlei）
> **项目**：AstrBot 插件 astrbot_plugin_nai_plus
> **开发语言**：Python

---

## 一、每次修改必须做的事（按顺序）

1. **修改代码**（功能/修复）
2. **更新 `CHANGELOG.md`** —— 在最上面加新版本的更新内容
3. **更新 `README.md`** —— 如果加了新配置项或新功能，必须写进去
4. **更新版本号**（在 `metadata.yaml`）—— 修改代码就要升版本号
   - 修复 bug → 升最后一位（v0.0.1 → v0.0.2）
   - 加小功能 → 升中间位（v0.0.2 → v0.1.0）
   - 大改动 → 升第一位（v0.x.x → v1.0.0）
5. **提交并推送到 GitHub** —— `git commit` + `git push origin main`

> ⚠️ **重要：打 stable 标签必须等用户确认！**
> - 发版后不能自己打 stable 标签，必须等用户测试后说"可以了/没问题了/确认好用了"才能打
> - 命令：`git tag vX.X.X-stable` + `git push origin vX.X.X-stable`
> - 没经过用户确认的版本不能叫 "stable"，只是发版而已

---

## 二、退回版本（用户说"退回去"）

- 用户说"退回去"三个字 = 退回到上一个 stable 标签
- 当前基线起点：`v0.0.1`
- 操作：
  1. `git checkout main`
  2. `git reset --hard <目标标签>`
  3. `git push origin main --force`

---

## 三、沟通注意事项

- **用户是零基础新手**：解释技术问题时要简单易懂，尽量用比喻
- **不要假设用户懂 git / 编程术语**
- **重要改动用列表列清楚**，不要写大段文字
- ⚠️ **stable 标签必须等用户确认才能打**，发版后不能自己就把它标成稳定版，必须等用户说"可以了/没问题了/确认好用了"才能打 stable 标签和改文档里的"最近稳定版本"

---

## 四、常见问题

### 插件配置里看不到新选项？
- 新配置项必须在 `_conf_schema.json` 里定义
- AstrBot 可能有缓存，试试"重载插件"或重启

### 版本号怎么升？
- 小修小补（改bug、优化）→ +0.0.1
- 加新功能 → +0.1.0
- 大改动不兼容 → +1.0.0

---

## 五、本文档更新记录

- **2026-09-18**：项目重新初始化，起点版本定义为 v0.0.1，更新文档与开发者信息。
